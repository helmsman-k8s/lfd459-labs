{"config":{"lang":["en"],"separator":"[\\s\\-]+","pipeline":["stopWordFilter"],"fields":{"title":{"boost":1000.0},"text":{"boost":1.0},"tags":{"boost":1000000.0}}},"docs":[{"location":"","title":"LFD459 - Kubernetes for Application Developers Lab Guide","text":"<p>Welcome to the student lab guide for LFD459 / CKAD preparation, delivered by VEGA TRAINING.</p>"},{"location":"#how-to-use-this-guide","title":"How to use this guide","text":"<p>Each chapter corresponds to a section of the LFD459 course. Follow the exercises in order - later chapters build on earlier ones.</p> <p>Work on your assigned lab nodes:</p> Node Hostname Role Control Plane <code>controller</code> Kubernetes control plane Worker 1 <code>worker1</code> Worker node Worker 2 <code>worker2</code> Worker node <p>Your username on all nodes is <code>guru</code>.</p>"},{"location":"#lab-topology","title":"Lab topology","text":"<ul> <li>Kubernetes version: 1.33.1</li> <li>Container runtime: containerd</li> <li>CNI: Calico</li> <li>OS: Ubuntu 22.04</li> </ul> <p>Cluster pre-installed</p> <p>The cluster is already installed and ready to use. You do not need to run any setup scripts. Start directly with the exercises.</p>"},{"location":"#quick-reference","title":"Quick reference","text":"<pre><code># Check cluster status\nkubectl get nodes\n\n# Check all pods across namespaces\nkubectl get pods --all-namespaces\n\n# Check component logs\njournalctl -u kubelet -f\n\n# Lab files location\nls ~/lfd459/\n</code></pre>"},{"location":"ch02_labs/","title":"Chapter 2 - Kubernetes Architecture","text":""},{"location":"ch02_labs/#lab-overview","title":"Lab Overview","text":"<p>In this chapter you will explore the Kubernetes cluster that has been pre-installed for you, create your first pods and services, and understand how the control plane manages workloads through deployments and ReplicaSets.</p>"},{"location":"ch02_labs/#exercise-21-verify-the-cluster","title":"Exercise 2.1: Verify the Cluster","text":"<p>The cluster is already installed and ready. Your first task is to confirm everything is healthy before starting the labs.</p> <ol> <li> <p>Connect to the controller node from your browser.</p> </li> <li> <p>Verify both worker nodes have joined the cluster and show a <code>Ready</code> state.</p> <pre><code>kubectl get nodes\n</code></pre> <p>Expected output:</p> <pre><code>NAME         STATUS   ROLES           AGE   VERSION\ncontroller   Ready    control-plane   Xm    v1.33.1\nworker1      Ready    worker          Xm    v1.33.1\nworker2      Ready    worker          Xm    v1.33.1\n</code></pre> <p>Worker node roles</p> <p>By default, worker nodes show <code>&lt;none&gt;</code> for the ROLES column. If your output shows <code>&lt;none&gt;</code>, label them with:</p> <pre><code>kubectl label node worker1 node-role.kubernetes.io/worker=worker\nkubectl label node worker2 node-role.kubernetes.io/worker=worker\n</code></pre> </li> <li> <p>Verify all system pods are running.</p> <pre><code>```bash\nkubectl get pods -n kube-system\n```\n</code></pre> <p>You should see Calico, CoreDNS, etcd, kube-apiserver, kube-controller-manager, kube-proxy and kube-scheduler all in <code>Running</code> state.</p> </li> <li> <p>Configure <code>kubectl</code> command-line completion for your current shell and persist it for future sessions.</p> <pre><code>source &lt;(kubectl completion bash)\necho \"source &lt;(kubectl completion bash)\" &gt;&gt; $HOME/.bashrc\n</code></pre> </li> <li> <p>Check whether the control-plane taint is present. In a training environment we remove it so pods can schedule on all nodes.</p> <pre><code>kubectl describe nodes | grep -i taint\n</code></pre> <p>If you see <code>node-role.kubernetes.io/control-plane:NoSchedule</code> on the controller node, remove it:</p> <pre><code>kubectl taint nodes --all node-role.kubernetes.io/control-plane-\n</code></pre> <p>Verify all nodes now show <code>&lt;none&gt;</code> for taints:</p> <pre><code>kubectl describe nodes | grep -i taint\n</code></pre> <pre><code>Taints:             &lt;none&gt;\nTaints:             &lt;none&gt;\nTaints:             &lt;none&gt;\n</code></pre> </li> </ol>"},{"location":"ch02_labs/#exercise-22-explore-kubectl-and-api-resources","title":"Exercise 2.2: Explore kubectl and API Resources","text":"<ol> <li> <p>Review the <code>kubectl</code> help output to become familiar with the available commands.</p> <pre><code>kubectl --help\n</code></pre> </li> <li> <p>Get a list of all available API resources and their API groups. Note that <code>pods</code> belongs to the stable <code>v1</code> group.</p> <pre><code>kubectl api-resources\n</code></pre> <p>Example output (truncated):</p> <pre><code>NAME                SHORTNAMES   APIVERSION   NAMESPACED   KIND\nconfigmaps          cm           v1           true         ConfigMap\npods                po           v1           true         Pod\nservices            svc          v1           true         Service\ndeployments         deploy       apps/v1      true         Deployment\nreplicasets         rs           apps/v1      true         ReplicaSet\n</code></pre> </li> <li> <p>Explore a specific sub-command using <code>--help</code>. For example, inspect the <code>taint</code> command.</p> <pre><code>kubectl taint --help\n</code></pre> </li> </ol>"},{"location":"ch02_labs/#exercise-23-create-a-basic-pod","title":"Exercise 2.3: Create a Basic Pod","text":"<ol> <li> <p>Move into the chapter lab directory.</p> <pre><code>cd ~/lfd459/ch02-architecture\nls\n</code></pre> <p>You should see: <code>basic.yaml</code>, <code>basic-later.yaml</code>, <code>basicservice.yaml</code>, <code>basicservice-AllDone.yaml</code>, <code>architecture-review1.yaml</code></p> </li> <li> <p>Review the minimal pod definition.</p> <pre><code>cat basic.yaml\n</code></pre> <pre><code>apiVersion: v1\nkind: Pod\nmetadata:\n  name: basicpod\nspec:\n  containers:\n  - name: webcont\n    image: nginx\n</code></pre> </li> <li> <p>Create the pod and verify it is running.</p> <pre><code>kubectl create -f basic.yaml\n</code></pre> <pre><code>pod/basicpod created\n</code></pre> <pre><code>kubectl get pod\n</code></pre> <pre><code>NAME       READY   STATUS    RESTARTS   AGE\nbasicpod   1/1     Running   0          23s\n</code></pre> </li> <li> <p>Inspect the pod details using <code>describe</code>. Look for the container image name and node assignment.</p> <pre><code>kubectl describe pod basicpod\n</code></pre> </li> <li> <p>Delete the pod and confirm it is gone.</p> <pre><code>kubectl delete pod basicpod\n</code></pre> <pre><code>pod \"basicpod\" deleted\n</code></pre> <pre><code>kubectl get pod\n</code></pre> <pre><code>No resources found in default namespace.\n</code></pre> </li> <li> <p>Edit <code>basic.yaml</code> to expose port 80. Add two lines at the same indentation as <code>image:</code>.</p> <pre><code>vim basic.yaml\n</code></pre> <p>The file should now look like this:</p> <pre><code>apiVersion: v1\nkind: Pod\nmetadata:\n  name: basicpod\nspec:\n  containers:\n  - name: webcont\n    image: nginx\n    ports:\n    - containerPort: 80\n</code></pre> </li> <li> <p>Re-create the pod and verify the internal IP address. Use <code>curl</code> to reach the nginx default page.</p> <pre><code>kubectl create -f basic.yaml\n</code></pre> <pre><code>pod/basicpod created\n</code></pre> <pre><code>kubectl get pod -o wide\n</code></pre> <pre><code>NAME       READY   STATUS    RESTARTS   AGE   IP            NODE      ...\nbasicpod   1/1     Running   0          18s   10.244.1.23   worker1   ...\n</code></pre> <pre><code>curl http://10.244.1.23\n</code></pre> <pre><code>&lt;!DOCTYPE html&gt;\n&lt;html&gt;\n&lt;head&gt;\n&lt;title&gt;Welcome to nginx!&lt;/title&gt;\n...\n</code></pre> <p>Note</p> <p>The pod IP comes from the Calico CNI CIDR (<code>10.244.0.0/16</code>). The exact address will differ in your environment.</p> <p>Delete the pod before continuing.</p> <pre><code>kubectl delete pod basicpod\n</code></pre> </li> <li> <p>Review the service definition.</p> <pre><code>cat basicservice.yaml\n</code></pre> <pre><code>apiVersion: v1\nkind: Service\nmetadata:\n  name: basicservice\nspec:\n  selector:\n    type: webserver\n  ports:\n  - protocol: TCP\n    port: 80\n</code></pre> </li> <li> <p>Edit <code>basic.yaml</code> to add a label that matches the service selector.</p> <pre><code>vim basic.yaml\n</code></pre> <p>Add the <code>labels</code> section under <code>metadata</code>:</p> <pre><code>apiVersion: v1\nkind: Pod\nmetadata:\n  name: basicpod\n  labels:\n    type: webserver     # &lt;-- add this\nspec:\n  containers:\n  - name: webcont\n    image: nginx\n    ports:\n    - containerPort: 80\n</code></pre> </li> <li> <p>Create both the pod and the service. Verify both are running.</p> <pre><code>kubectl create -f basic.yaml\n</code></pre> <pre><code>pod/basicpod created\n</code></pre> <pre><code>kubectl create -f basicservice.yaml\n</code></pre> <pre><code>service/basicservice created\n</code></pre> <pre><code>kubectl get pod\n</code></pre> <pre><code>NAME       READY   STATUS    RESTARTS   AGE\nbasicpod   1/1     Running   0          110s\n</code></pre> <pre><code>kubectl get svc\n</code></pre> <pre><code>NAME           TYPE        CLUSTER-IP      EXTERNAL-IP   PORT(S)   AGE\nbasicservice   ClusterIP   10.96.112.50    &lt;none&gt;        80/TCP    14s\nkubernetes     ClusterIP   10.96.0.1       &lt;none&gt;        443/TCP   4h\n</code></pre> </li> <li> <p>Test access to the webserver via the service <code>CLUSTER-IP</code>.</p> <pre><code>curl http://10.96.112.50\n</code></pre> <pre><code>&lt;!DOCTYPE html&gt;\n&lt;html&gt;\n&lt;head&gt;\n&lt;title&gt;Welcome to nginx!&lt;/title&gt;\n...\n</code></pre> </li> <li> <p>Now expose the service outside the cluster using <code>NodePort</code>. Delete the existing service and update the YAML.</p> <pre><code>kubectl delete svc basicservice\n</code></pre> <pre><code>service \"basicservice\" deleted\n</code></pre> <p>Review <code>basicservice-AllDone.yaml</code> - it already has <code>type: NodePort</code> added:</p> <pre><code>cat basicservice-AllDone.yaml\n</code></pre> <pre><code>apiVersion: v1\nkind: Service\nmetadata:\n  name: basicservice\nspec:\n  selector:\n    type: webserver\n  type: NodePort\n  ports:\n  - protocol: TCP\n    port: 80\n</code></pre> <p>Create the service using this file.</p> <pre><code>kubectl create -f basicservice-AllDone.yaml\n</code></pre> <pre><code>service/basicservice created\n</code></pre> <pre><code>kubectl get svc\n</code></pre> <pre><code>NAME           TYPE        CLUSTER-IP       EXTERNAL-IP   PORT(S)        AGE\nbasicservice   NodePort    10.100.139.155   &lt;none&gt;        80:31514/TCP   3s\nkubernetes     ClusterIP   10.96.0.1        &lt;none&gt;        443/TCP        47h\n</code></pre> </li> <li> <p>Note the high-numbered NodePort (e.g. <code>31514</code>). Test access from the controller node using any node's IP.</p> <pre><code>curl http://worker1:31514\n</code></pre> <pre><code>&lt;!DOCTYPE html&gt;\n...\n</code></pre> <p>Tip</p> <p>Use <code>kubectl get nodes -o wide</code> to find the node IPs.</p> </li> </ol>"},{"location":"ch02_labs/#exercise-24-multi-container-pods","title":"Exercise 2.4: Multi-Container Pods","text":"<ol> <li> <p>Edit <code>basic.yaml</code> to add a second container acting as a logging sidecar. Add the <code>fdlogger</code> container after the <code>webcont</code> container - the dash should align with the previous container dash.</p> <pre><code>vim basic.yaml\n</code></pre> <pre><code>...\ncontainers:\n- name: webcont\n  image: nginx\n  ports:\n  - containerPort: 80\n- name: fdlogger\n  image: fluentd\n</code></pre> </li> <li> <p>Delete and recreate the pod. This time <code>READY</code> should show <code>2/2</code>.</p> <pre><code>kubectl delete pod basicpod ; kubectl create -f basic.yaml\n</code></pre> <pre><code>pod \"basicpod\" deleted\npod/basicpod created\n</code></pre> <pre><code>kubectl get pod\n</code></pre> <pre><code>NAME       READY   STATUS    RESTARTS   AGE\nbasicpod   2/2     Running   0          2m8s\n</code></pre> </li> <li> <p>Verify the <code>fdlogger</code> container is present in the describe output.</p> <pre><code>kubectl describe pod basicpod\n</code></pre> <p>Look for the <code>fdlogger</code> section listing image, container ID and state.</p> </li> <li> <p>Shut down the pod. We will revisit it with persistent storage in Chapter 5.</p> <pre><code>kubectl delete pod basicpod\n</code></pre> <pre><code>pod \"basicpod\" deleted\n</code></pre> </li> </ol>"},{"location":"ch02_labs/#exercise-25-create-a-simple-deployment","title":"Exercise 2.5: Create a Simple Deployment","text":"<ol> <li> <p>Create a Deployment running the nginx webserver. A Deployment gives you scalability, self-healing, and rolling updates.</p> <pre><code>kubectl create deployment firstpod --image=nginx\n</code></pre> <pre><code>deployment.apps/firstpod created\n</code></pre> </li> <li> <p>Verify the Deployment and its managed pod are both running.</p> <pre><code>kubectl get deployment,pod\n</code></pre> <pre><code>NAME                       READY   UP-TO-DATE   AVAILABLE   AGE\ndeployment.apps/firstpod   1/1     1            1           10s\n\nNAME                            READY   STATUS    RESTARTS   AGE\npod/firstpod-65c7f8b5bb-zmlp8   1/1     Running   0          10s\n</code></pre> </li> <li> <p>Describe the Deployment, then the Pod. Note the labels, replica count, strategy type, and the node the pod is scheduled on.</p> <pre><code>kubectl describe deployment firstpod\nkubectl describe pod &lt;pod-name&gt;\n</code></pre> <p>Tip</p> <p>Use Tab completion to avoid typing the full auto-generated pod name.</p> </li> <li> <p>List available namespaces.</p> <pre><code>kubectl get namespaces\n</code></pre> <pre><code>NAME              STATUS   AGE\ndefault           Active   20m\nkube-node-lease   Active   20m\nkube-public       Active   20m\nkube-system       Active   20m\n</code></pre> </li> <li> <p>View all pods in the <code>kube-system</code> namespace.</p> <pre><code>kubectl get pod -n kube-system\n</code></pre> <p>You should see Calico, CoreDNS, etcd, and other control plane components.</p> </li> <li> <p>Query a namespace that does not exist. Note no error is returned - simply no resources found.</p> <pre><code>kubectl get pod -n fakenamespace\n</code></pre> <pre><code>No resources found in fakenamespaces namespace.\n</code></pre> </li> <li> <p>View pods across all namespaces at once.</p> <pre><code>kubectl get pod --all-namespaces\n</code></pre> </li> <li> <p>View multiple resource types in a single command. Note the short names: <code>deploy</code>, <code>rs</code>, <code>po</code>, <code>svc</code>, <code>ep</code>.</p> <pre><code>kubectl get deploy,rs,po,svc,ep\n</code></pre> </li> <li> <p>Delete the ReplicaSet and immediately check the resources again. You will see a new ReplicaSet and pod created automatically by the Deployment operator.</p> <pre><code>kubectl delete rs &lt;replicaset-name&gt;\n</code></pre> <pre><code>replicaset.apps \"firstpod-65c7f8b5bb\" deleted\n</code></pre> <pre><code>kubectl get deploy,rs,po\n</code></pre> <p>Notice the age on the new ReplicaSet and pod is only a few seconds - the Deployment reconciled immediately.</p> </li> <li> <p>Delete the top-level Deployment. After about 30 seconds everything it manages cascades down.</p> <pre><code>kubectl delete deployment firstpod\n</code></pre> <pre><code>deployment.apps \"firstpod\" deleted\n</code></pre> <pre><code>kubectl get deployment,rs,po,svc,ep\n</code></pre> <p>Only the cluster services and endpoints remain.</p> </li> <li> <p>Clean up the <code>basicservice</code> service.</p> <pre><code>kubectl delete svc basicservice\n</code></pre> <pre><code>service \"basicservice\" deleted\n</code></pre> </li> </ol>"},{"location":"ch02_labs/#exercise-26-domain-review","title":"Exercise 2.6: Domain Review","text":"<p>Important</p> <p>The official CKAD curriculum and exam handbook can change at any time. Always check the current version before your exam.</p> <ol> <li>Using a browser, visit https://www.cncf.io/certification/ckad/ and read through the program description.</li> <li>Open the Curriculum Overview and Candidate Handbook in separate tabs and read both in full.</li> <li>In the Curriculum Overview, find the Application Design and Build domain. Notice the sub-topics - many will be covered in the upcoming chapters.</li> <li>In the Candidate Handbook, find the Important Instructions: CKA and CKAD section. Note the Kubernetes version for the exam and the list of allowed resources.</li> <li> <p>Practice speed: using a timer, see how long it takes you to create and verify each of the following, then repeat to get faster:</p> <ul> <li>A new pod with the <code>nginx</code> image, showing all containers running and <code>Ready</code> status.</li> <li>A new service exposing the pod as a <code>NodePort</code>, with a working webserver reachable on the NodePort.</li> <li>Update the pod to run the <code>nginx:1.28-alpine</code> image and re-verify access via NodePort.</li> </ul> </li> <li> <p>Use the <code>architecture-review1.yaml</code> file to practice troubleshooting. Create the pod and determine why it does not start.</p> <pre><code>kubectl create -f architecture-review1.yaml\nkubectl get pod\nkubectl describe pod break1\n</code></pre> <p>Hint</p> <p>Look carefully at the image name in the YAML. Use <code>kubectl describe</code> to read the event messages.</p> </li> <li> <p>Once you have fixed and verified the pod, clean up before moving to the next chapter.</p> <pre><code>kubectl delete -f architecture-review1.yaml\nkubectl delete pod basicpod --ignore-not-found\nkubectl delete svc basicservice --ignore-not-found\n</code></pre> </li> </ol>"},{"location":"ch03_labs/","title":"Chapter 3 - Build","text":""},{"location":"ch03_labs/#lab-overview","title":"Lab Overview","text":"<p>In this chapter you will containerize a simple Python application using Podman, set up a local container registry inside the cluster, push your image to it, deploy the application with multiple replicas, and configure readiness and liveness probes to manage container health.</p>"},{"location":"ch03_labs/#exercise-31-deploy-a-new-application","title":"Exercise 3.1: Deploy a New Application","text":""},{"location":"ch03_labs/#working-with-a-simple-python-script","title":"Working with a Simple Python Script","text":"<ol> <li> <p>Connect to the controller node. Verify Python 3 is installed.</p> <pre><code>sudo apt-get -y install python3\n</code></pre> <p>It should report already installed on Ubuntu 22.04.</p> </li> <li> <p>Move into the chapter lab directory.</p> <pre><code>cd ~/lfd459/ch03-build\nls\n</code></pre> <p>You should see: <code>simple.py</code>, <code>Dockerfile</code>, <code>easyregistry.yaml</code>, <code>local-repo-setup.sh</code>, <code>edited-simpleapp.yaml</code>, <code>edited-later-simpleapp.yaml</code>, <code>build-review1.yaml</code></p> </li> <li> <p>Create a working directory for the application and move into it.</p> <pre><code>mkdir app1\ncd app1\n</code></pre> </li> <li> <p>Copy <code>simple.py</code> into the app1 directory and review it.</p> <pre><code>cp ../simple.py .\ncat simple.py\n</code></pre> <p>The script runs in an infinite loop, writing the hostname and current timestamp to a file called <code>date.out</code> every 5 seconds.</p> </li> <li> <p>Make the script executable and run it briefly to verify it works. Use Ctrl-C to stop it after 15-20 seconds.</p> <pre><code>chmod +x simple.py\n</code></pre> <pre><code>./simple.py\n^CTraceback (most recent call last):\n  File \"./simple.py\", line 24, in &lt;module&gt;\n    time.sleep(5)\nKeyboardInterrupt\n</code></pre> </li> <li> <p>Verify the output file was created with hostname and timestamp entries.</p> <pre><code>cat date.out\n</code></pre> <pre><code>2026-03-18 19:27:50\ncontroller\n2026-03-18 19:27:55\ncontroller\n...\n</code></pre> </li> <li> <p>Copy the <code>Dockerfile</code> into the app1 directory and review it.</p> <pre><code>cp ../Dockerfile .\ncat Dockerfile\n</code></pre> <pre><code>FROM docker.io/library/python:3\nADD simple.py /\n## RUN pip install pystrich\nCMD [ \"python\", \"./simple.py\" ]\n</code></pre> <p>Note</p> <p>The filename must be exactly <code>Dockerfile</code> - capital D, no extension.</p> </li> <li> <p>Install Podman. Podman is a daemonless container tool compatible with Docker syntax.</p> <pre><code>sudo apt-get install -y podman\n</code></pre> </li> <li> <p>Build the container image. The dot (<code>.</code>) at the end means \"use the current directory as the build context\". When prompted to choose a registry for the <code>python</code> base image, select <code>docker.io</code>.</p> <pre><code>sudo podman build -t simpleapp .\n</code></pre> <pre><code>STEP 1/3: FROM docker.io/library/python:3\nTrying to pull docker.io/library/python:3...\n...\nSuccessfully tagged localhost/simpleapp:latest\n</code></pre> </li> <li> <p>Verify the new image appears in the local image list.</p> <pre><code>sudo podman images\n</code></pre> <pre><code>REPOSITORY              TAG     IMAGE ID      CREATED        SIZE\nlocalhost/simpleapp     latest  11d4607c72e0  7 seconds ago  1.04 GB\ndocker.io/library/python  3     58a8f3dcd68a  3 days ago     1.04 GB\n</code></pre> </li> <li> <p>Run the container image with Podman. The script will run silently in the foreground. Wait 15 seconds then use Ctrl-C to stop it.</p> <pre><code>sudo podman run localhost/simpleapp\n</code></pre> <pre><code>^CTraceback (most recent call last):\n  File \"./simple.py\", line 24, in &lt;module&gt;\n    time.sleep(5)\nKeyboardInterrupt\n</code></pre> </li> <li> <p>Locate the <code>date.out</code> file that Podman created inside the container's overlay filesystem. Note the container hostname is a random hash, not <code>controller</code>.</p> <pre><code>sudo find / -name date.out\n</code></pre> <pre><code>/home/guru/app1/date.out\n/var/lib/containers/storage/overlay/.../diff/date.out\n</code></pre> </li> <li> <p>View the contents of the container's <code>date.out</code> file. Use the full path returned by the find command above.</p> <pre><code>sudo tail /var/lib/containers/storage/overlay/&lt;hash&gt;/diff/date.out\n</code></pre> <pre><code>2026-03-18 19:32:20\nb8cec851efca\n2026-03-18 19:32:25\nb8cec851efca\n</code></pre> </li> </ol>"},{"location":"ch03_labs/#exercise-32-configure-a-local-registry","title":"Exercise 3.2: Configure a Local Registry","text":"<p>Rather than pushing to Docker Hub, we will deploy a private registry inside the cluster and make it available to all nodes.</p> <ol> <li> <p>Return to the chapter directory and create the local registry using the provided YAML.</p> <pre><code>cd ~/lfd459/ch03-build\nkubectl create -f easyregistry.yaml\n</code></pre> <pre><code>service/nginx created\nservice/registry created\ndeployment.apps/nginx created\npersistentvolumeclaim/nginx-claim0 created\ndeployment.apps/registry created\npersistentvolumeclaim/registry-claim0 created\npersistentvolume/vol1 created\npersistentvolume/vol2 created\n</code></pre> </li> <li> <p>Note the ClusterIP assigned to the registry service. In this setup it is expected to be <code>10.97.40.62</code> - verify this matches.</p> <pre><code>kubectl get svc | grep registry\n</code></pre> <pre><code>registry   ClusterIP   10.97.40.62   &lt;none&gt;   5000/TCP   5m35s\n</code></pre> <p>If the ClusterIP is different</p> <p>The <code>easyregistry.yaml</code> hardcodes <code>clusterIP: 10.97.40.62</code>. If a different IP was assigned, delete the service, edit the YAML to match the actual IP, and recreate it. The <code>local-repo-setup.sh</code> script also uses this same IP - edit it before running if needed.</p> </li> <li> <p>Verify the registry is responding.</p> <pre><code>curl 10.97.40.62:5000/v2/_catalog\n</code></pre> <pre><code>{\"repositories\":[]}\n</code></pre> </li> <li> <p>Configure containerd and Podman on the controller to use the local registry over HTTP. Run the setup script (note the dot-space to source it and export the <code>$repo</code> variable into your shell).</p> <pre><code>chmod +x local-repo-setup.sh\n. ./local-repo-setup.sh\n</code></pre> <pre><code>Configuring local repo, Please standby\n...\nLocal Repo configured, follow the next steps\n</code></pre> </li> <li> <p>Pull a small test image from Docker Hub, tag it for the local registry, and push it.</p> <pre><code>sudo podman pull docker.io/library/alpine\nsudo podman tag alpine $repo/tagtest\nsudo podman push $repo/tagtest\n</code></pre> <pre><code>Getting image source signatures\nCopying blob b2d5eeeaba3a done\n...\n</code></pre> </li> <li> <p>Remove the local cached copies of the images, then pull from your local registry to verify it works end-to-end.</p> <pre><code>sudo podman image rm alpine\nsudo podman image rm $repo/tagtest\nsudo podman pull $repo/tagtest\n</code></pre> <pre><code>Trying to pull 10.97.40.62:5000/tagtest:latest...\n...\n6dbb9cc54074106d46d4ccb330f2a40a682d49dda5f4844962b7dce9fe44aaec\n</code></pre> </li> <li> <p>Connect to worker1 in a separate terminal. Install Podman and configure the local registry on the worker node the same way.</p> <pre><code>sudo apt-get install -y podman\nscp guru@controller:~/lfd459/ch03-build/local-repo-setup.sh ~/\nchmod +x ~/local-repo-setup.sh\n. ~/local-repo-setup.sh\n</code></pre> <p>Verify the worker can pull from the registry.</p> <pre><code>sudo podman pull $repo/tagtest\n</code></pre> <pre><code>Trying to pull 10.97.40.62:5000/tagtest:latest...\n...\n</code></pre> <p>Repeat on worker2.</p> </li> <li> <p>Return to the controller. Tag and push the <code>simpleapp</code> image to the local registry.</p> <pre><code>cd app1\nsudo podman tag simpleapp $repo/simpleapp\nsudo podman push $repo/simpleapp\n</code></pre> <pre><code>Getting image source signatures\nCopying blob 47458fb45d99 done\n...\n</code></pre> </li> <li> <p>Verify both images appear in the registry catalog from the controller.</p> <pre><code>curl $repo/v2/_catalog\n</code></pre> <pre><code>{\"repositories\":[\"simpleapp\",\"tagtest\"]}\n</code></pre> </li> <li> <p>Deploy the simpleapp from the local registry and scale it to 6 replicas.</p> <pre><code>kubectl create deployment try1 --image=$repo/simpleapp\n</code></pre> <pre><code>deployment.apps/try1 created\n</code></pre> <pre><code>kubectl scale deployment try1 --replicas=6\n</code></pre> <pre><code>deployment.apps/try1 scaled\n</code></pre> <pre><code>kubectl get pod -o wide\n</code></pre> <pre><code>NAME                   READY  STATUS   RESTARTS  AGE  IP              NODE\ntry1-55f675ddd-28vgs   1/1    Running  0         17s  10.244.1.30     worker1\ntry1-55f675ddd-4vzt7   1/1    Running  0         17s  10.244.2.100    worker2\ntry1-55f675ddd-2nrsj   1/1    Running  0         17s  10.244.0.26     controller\n...\n</code></pre> <p>Pods should spread across all three nodes.</p> </li> <li> <p>On worker1, verify the simpleapp containers are running via crictl.</p> <pre><code>sudo crictl config \\\n--set runtime-endpoint=unix:///run/containerd/containerd.sock \\\n--set image-endpoint=unix:///run/containerd/containerd.sock\n</code></pre> <pre><code>sudo crictl ps | grep simple\n</code></pre> </li> <li> <p>Return to the controller. Save the try1 deployment as a YAML file.</p> <pre><code>kubectl get deployment try1 -o yaml &gt; simpleapp.yaml\n</code></pre> </li> <li> <p>Delete and recreate the deployment from the saved YAML. Verify 6/6 replicas are running.</p> <pre><code>kubectl delete deployment try1\n</code></pre> <pre><code>deployment.apps \"try1\" deleted\n</code></pre> <pre><code>kubectl create -f simpleapp.yaml\n</code></pre> <pre><code>deployment.apps/try1 created\n</code></pre> <pre><code>kubectl get deployment\n</code></pre> <pre><code>NAME       READY   UP-TO-DATE   AVAILABLE   AGE\nnginx      1/1     1            1           15m\nregistry   1/1     1            1           15m\ntry1       6/6     6            6           5s\n</code></pre> </li> </ol>"},{"location":"ch03_labs/#exercise-33-configure-probes","title":"Exercise 3.3: Configure Probes","text":""},{"location":"ch03_labs/#readinessprobe","title":"readinessProbe","text":"<p>A <code>readinessProbe</code> tells Kubernetes when a container is ready to accept traffic. Until the probe succeeds, the pod is not sent any requests.</p> <ol> <li> <p>Edit <code>simpleapp.yaml</code> and add a <code>readinessProbe</code> to the <code>simpleapp</code> container. The probe runs <code>cat /tmp/healthy</code> - the container is not considered ready until that file exists.</p> <pre><code>vim simpleapp.yaml\n</code></pre> <p>Find the <code>containers:</code> section for the simpleapp container and add the <code>readinessProbe</code> block after the <code>imagePullPolicy</code> line:</p> <pre><code>spec:\n  containers:\n  - image: 10.97.40.62:5000/simpleapp\n    imagePullPolicy: Always\n    name: simpleapp\n    readinessProbe:\n      periodSeconds: 5\n      exec:\n        command:\n        - cat\n        - /tmp/healthy\n    resources: {}\n</code></pre> <p>Tip</p> <p>The <code>edited-simpleapp.yaml</code> file in your working directory already has this probe added. You can reference it for the correct indentation, but remember to verify the registry IP matches your environment.</p> </li> <li> <p>Delete and recreate the deployment.</p> <pre><code>kubectl delete deployment try1\n</code></pre> <pre><code>deployment.apps \"try1\" deleted\n</code></pre> <pre><code>kubectl create -f simpleapp.yaml\n</code></pre> <pre><code>deployment.apps/try1 created\n</code></pre> </li> <li> <p>The deployment shows 6 pods but 0 available - all are waiting for <code>/tmp/healthy</code> to exist.</p> <pre><code>kubectl get deployment\n</code></pre> <pre><code>NAME       READY   UP-TO-DATE   AVAILABLE   AGE\ntry1       0/6     6            0           15s\n</code></pre> </li> <li> <p>Check the pods. Each try1 pod shows <code>0/1</code> in READY - the readinessProbe is failing.</p> <pre><code>kubectl get pods\n</code></pre> <pre><code>NAME                    READY   STATUS    RESTARTS   AGE\ntry1-9869bdb88-rtchc    0/1     Running   0          26s\ntry1-9869bdb88-2wfnr    0/1     Running   0          26s\n...\n</code></pre> </li> <li> <p>Exec into one pod and create the health check file.</p> <pre><code>kubectl exec -it try1-9869bdb88-rtchc -- /bin/bash\n</code></pre> <pre><code>root@try1-9869bdb88-rtchc:/# touch /tmp/healthy\nroot@try1-9869bdb88-rtchc:/# exit\n</code></pre> </li> <li> <p>Wait at least 5 seconds (the <code>periodSeconds</code>), then check pods again. The pod you touched should now show <code>1/1</code>.</p> <pre><code>kubectl get pods\n</code></pre> <pre><code>NAME                    READY   STATUS    RESTARTS   AGE\ntry1-9869bdb88-rtchc    1/1     Running   0          4m\ntry1-9869bdb88-2wfnr    0/1     Running   0          4m\n...\n</code></pre> </li> <li> <p>Use a <code>for</code> loop to touch <code>/tmp/healthy</code> in all remaining pods at once.</p> <pre><code>for name in $(kubectl get pods -l app=try1 -o name); \\\ndo kubectl exec $name -- touch /tmp/healthy; done\n</code></pre> </li> <li> <p>Wait a few seconds, then verify all 6 pods are now <code>1/1 Ready</code>.</p> <pre><code>kubectl get pods\n</code></pre> <pre><code>NAME                    READY   STATUS    RESTARTS   AGE\ntry1-9869bdb88-rtchc    1/1     Running   0          22m\ntry1-9869bdb88-2wfnr    1/1     Running   0          22m\n...\n</code></pre> </li> </ol>"},{"location":"ch03_labs/#livenessprobe-and-sidecar-container","title":"livenessProbe and Sidecar Container","text":"<ol> <li> <p>Edit <code>simpleapp.yaml</code> again to add a <code>livenessProbe</code> and a second sidecar container (<code>goproxy</code>). Find the end of the simpleapp container definition and add the goproxy container after it.</p> <pre><code>vim simpleapp.yaml\n</code></pre> <p>Add after <code>terminationMessagePolicy: File</code>:</p> <pre><code>    - name: goproxy\n      image: registry.k8s.io/goproxy:0.1\n      ports:\n      - containerPort: 8080\n      readinessProbe:\n        tcpSocket:\n          port: 8080\n        initialDelaySeconds: 5\n        periodSeconds: 10\n      livenessProbe:\n        tcpSocket:\n          port: 8080\n        initialDelaySeconds: 15\n        periodSeconds: 20\n</code></pre> <p>Tip</p> <p>The <code>edited-later-simpleapp.yaml</code> file shows the complete deployment with both containers configured. Use it as a reference for indentation.</p> </li> <li> <p>Delete and recreate the deployment.</p> <pre><code>kubectl delete deployment try1\n</code></pre> <pre><code>deployment.apps \"try1\" deleted\n</code></pre> <pre><code>kubectl create -f simpleapp.yaml\n</code></pre> <pre><code>deployment.apps/try1 created\n</code></pre> </li> <li> <p>Check the pods. Each pod now has two containers (<code>READY 1/2</code> or <code>0/2</code>). The goproxy container starts, but simpleapp is not ready again because <code>/tmp/healthy</code> is missing.</p> <pre><code>kubectl get pods\n</code></pre> <pre><code>NAME                      READY   STATUS    RESTARTS   AGE\ntry1-76cc5ffcc6-4rjvh     1/2     Running   0          3s\ntry1-76cc5ffcc6-bk5f5     1/2     Running   0          3s\ntry1-76cc5ffcc6-d8n5q     0/2     Running   0          3s\n...\n</code></pre> </li> <li> <p>Create the health check file in all pods. Specify the <code>-c simpleapp</code> flag to target the correct container.</p> <pre><code>for name in $(kubectl get pods -l app=try1 -o name); \\\ndo kubectl exec $name -c simpleapp -- touch /tmp/healthy; done\n</code></pre> </li> <li> <p>Wait a minute and verify all pods show <code>2/2 Running</code>.</p> <pre><code>kubectl get pods\n</code></pre> <pre><code>NAME                      READY   STATUS    RESTARTS   AGE\ntry1-76cc5ffcc6-4rjvh     2/2     Running   0          ~1m\ntry1-76cc5ffcc6-bk5f5     2/2     Running   0          ~1m\n...\n</code></pre> </li> <li> <p>Examine the events for one of the pods. Note the <code>Readiness probe failed</code> warnings from before the file was created.</p> <pre><code>kubectl describe pod try1-76cc5ffcc6-4rjvh | tail -20\n</code></pre> <p>Look for <code>Warning Unhealthy</code> events showing <code>cat: /tmp/healthy: No such file or directory</code>.</p> </li> <li> <p>Confirm both containers in the pod show <code>State: Running</code> and <code>Ready: True</code>.</p> <pre><code>kubectl describe pod try1-76cc5ffcc6-4rjvh | grep -E 'State|Ready'\n</code></pre> <pre><code>State:          Running\nReady:          True\nState:          Running\nReady:          True\nReady:          True\nContainersReady: True\n</code></pre> <p>Troubleshooting</p> <p>If the try1 deployment seems stuck, check the registry catalog and re-push if needed, then scale down to 0 and back to 6:</p> <pre><code>curl $repo/v2/_catalog\nsudo podman push $repo/simpleapp\nkubectl scale deployment try1 --replicas 0\nkubectl scale deployment try1 --replicas 6\n</code></pre> </li> </ol>"},{"location":"ch03_labs/#exercise-34-domain-review","title":"Exercise 3.4: Domain Review","text":"<p>Important</p> <p>Source pages and content in this review can change at any time. Always check current information.</p> <p>Revisit the CKAD curriculum and locate the topics covered in this chapter:</p> <ul> <li>Implement probes and health checks</li> <li>Define, build and modify container images</li> <li>Understand multi-container Pod design patterns</li> <li> <p>Utilize container logs</p> </li> <li> <p>Using the three documentation sources allowed during the CKAD exam, find and bookmark working YAML examples for <code>livenessProbe</code>, <code>readinessProbe</code>, and multi-container pods.</p> </li> <li>Deploy a new nginx webserver. Add both a <code>livenessProbe</code> and a <code>readinessProbe</code> on port 80. Test that both probes and the webserver are working.</li> <li> <p>Use <code>build-review1.yaml</code> to create a broken deployment. Fix it so both containers are running and in a <code>READY</code> state.</p> <p><code>bash</code></p> <pre><code>```bash\n</code></pre> <p>kubectl create -f build-review1.yaml     ```</p> <p>Hint</p> <p>The web server (<code>nginx</code>) listens on port 80. The proxy (<code>goproxy</code>) listens on port 8080. Examine the probe configurations carefully - one of the port numbers is wrong.</p> </li> <li> <p>Once fixed, access the default nginx page and verify the GET request appears in the container log.</p> <p>Get the pod IP:</p> <pre><code>kubectl get pod -l app=break2 -o wide\n</code></pre> <p>Then curl the pod IP and check the log:</p> <pre><code>curl http://&lt;pod-ip from above&gt;\nkubectl logs &lt;pod-name&gt; -c brokenapp\n</code></pre> <p>You should see a line like:</p> <pre><code>192.168.124.0 - - [21/Mar/2026:03:30:31 +0000] \"GET / HTTP/1.1\" 200 612 \"-\" \"curl/7.58.0\"\n</code></pre> </li> <li> <p>Clean up all resources created in this review before moving on.</p> <pre><code>kubectl delete deployment try1 nginx registry break2 --ignore-not-found\nkubectl delete svc nginx registry --ignore-not-found\nkubectl delete pvc nginx-claim0 registry-claim0 --ignore-not-found\nkubectl delete pv vol1 vol2 --ignore-not-found\n</code></pre> </li> </ul>"},{"location":"ch04_labs/","title":"Chapter 4 - Design","text":""},{"location":"ch04_labs/#lab-overview","title":"Lab Overview","text":"<p>In this chapter you will review CNI network plugin concepts, work with Jobs and CronJobs for time-limited workloads, use label selectors to manage pods, set CPU and memory resource limits, and explore initContainers and Custom Resource Definitions.</p>"},{"location":"ch04_labs/#exercise-41-planning-the-deployment","title":"Exercise 4.1: Planning the Deployment","text":""},{"location":"ch04_labs/#quick-understanding-of-network-plugins","title":"Quick Understanding of Network Plugins","text":"<p>While application developers don't configure cluster networking, understanding the CNI plugin in use helps when troubleshooting and designing network policies.</p> <ol> <li> <p>Connect to the controller node. Verify the CNI plugin is active by reading the controller manager startup logs. Use Tab completion for the pod name.</p> <pre><code>kubectl -n kube-system logs kube-controller-manager-controller\n</code></pre> <p>Look for lines referencing the network plugin initialization.</p> </li> <li> <p>Check the kube-proxy logs to see which proxy mode is active.</p> <pre><code>kubectl -n kube-system logs kube-proxy-&lt;TAB&gt;\n</code></pre> <p>Our CNI: Calico</p> <p>This cluster uses Calico as the CNI plugin. Calico provides a layer 3 network using BGP by default and fully supports NetworkPolicies.</p> </li> <li> <p>Review the properties of common CNI plugins using the links below, then answer the questions that follow.</p> <ul> <li>Flannel: https://github.com/coreos/flannel</li> <li>Calico: https://docs.projectcalico.org/</li> <li>Cilium: https://cilium.io/</li> <li>Kube Router: https://www.kube-router.io</li> </ul> <p>Answer the following questions:</p> <ul> <li>Which plugins support vxlans?</li> <li>Which are layer 2 plugins?</li> <li>Which are layer 3?</li> <li>Which support network policies?</li> <li>Which can encrypt all TCP and UDP traffic?</li> </ul> </li> </ol> Plugin Answers <ul> <li>vxlans: Canal, Calico, Flannel, Weave Net, Cilium</li> <li>Layer 2: Canal, Weave Net</li> <li>Layer 3: Calico, Romana, Kube Router</li> <li>Network policies: Calico, Canal, Kube Router, Weave Net, Cilium</li> <li>Encrypt TCP/UDP: Calico, Weave Net, Cilium</li> </ul> <p>Note</p> <p>Flannel does not support network policies natively. Calico, which this cluster uses, does. Keep this in mind in Chapter 6.</p>"},{"location":"ch04_labs/#multi-container-pod-considerations","title":"Multi-Container Pod Considerations","text":"<p>Consider the following questions based on what you learned in this chapter:</p> <ol> <li>Which deployment method allows the most flexibility - one app per pod or multiple?</li> <li>Which allows the most granular scalability?</li> <li>Which has the best inter-container performance?</li> <li>How many IP addresses are assigned per pod?</li> <li>What are some ways containers can communicate within the same pod?</li> <li>What are some reasons to use multiple containers per pod?</li> </ol> Multi-Pod Answers <ol> <li>One per pod - most flexible</li> <li>One per pod - most granular scalability</li> <li>Multiple per pod - best inter-container performance</li> <li>One IP per pod</li> <li>IPC, loopback interface, shared filesystem</li> <li>Lean containers may lack logging or other features. Adding sidecar, ambassador, or adapter containers provides that functionality without modifying the primary container.</li> </ol>"},{"location":"ch04_labs/#exercise-42-create-a-job","title":"Exercise 4.2: Create a Job","text":"<p>Jobs run a container a set number of times to completion, rather than keeping it running continuously.</p> <ol> <li> <p>Move into the chapter lab directory.</p> <pre><code>cd ~/lfd459/ch04-design\nls\n</code></pre> </li> <li> <p>Review and create the basic job definition.</p> <pre><code>cat job.yaml\n</code></pre> <pre><code>apiVersion: batch/v1\nkind: Job\nmetadata:\n  name: sleepy\nspec:\n  template:\n    spec:\n      containers:\n      - name: resting\n        image: busybox\n        command: [\"/bin/sleep\"]\n        args: [\"3\"]\n      restartPolicy: Never\n</code></pre> <pre><code>kubectl create -f job.yaml\n</code></pre> <pre><code>job.batch/sleepy created\n</code></pre> </li> <li> <p>Verify the job completes successfully. Check while it is running, then again after it finishes.</p> <pre><code>kubectl get job\n</code></pre> <pre><code>NAME     COMPLETIONS   DURATION   AGE\nsleepy   0/1           3s         3s\n</code></pre> <pre><code>kubectl get job\n</code></pre> <pre><code>NAME     COMPLETIONS   DURATION   AGE\nsleepy   1/1           7s         11s\n</code></pre> </li> <li> <p>Describe the job to see its full details including parallelism, completions, and pod statuses.</p> <pre><code>kubectl describe jobs.batch sleepy\n</code></pre> </li> <li> <p>Inspect the default job parameters in YAML output. Note <code>backoffLimit: 6</code>, <code>completions: 1</code>, <code>parallelism: 1</code>.</p> <pre><code>kubectl get jobs.batch sleepy -o yaml | grep -A5 \"^spec:\"\n</code></pre> </li> <li> <p>Delete the job.</p> <pre><code>kubectl delete jobs.batch sleepy\n</code></pre> <pre><code>job.batch \"sleepy\" deleted\n</code></pre> </li> <li> <p>Edit <code>job.yaml</code> and add <code>completions: 5</code> to run the job five times sequentially.</p> <pre><code>vim job.yaml\n</code></pre> <pre><code>spec:\n  completions: 5        # &lt;-- add this line\n  template:\n</code></pre> <p>Tip</p> <p>If you prefer not to edit manually, use the pre-built version: <code>kubectl create -f job-v2.yaml</code></p> </li> <li> <p>Create the job again and watch the completions count up.</p> <pre><code>kubectl create -f job.yaml\nkubectl get jobs.batch\n</code></pre> <pre><code>NAME     COMPLETIONS   DURATION   AGE\nsleepy   0/5           5s         5s\n</code></pre> </li> <li> <p>Once all 5 complete, delete the job.</p> <pre><code>kubectl get jobs.batch\n</code></pre> <pre><code>NAME     COMPLETIONS   DURATION   AGE\nsleepy   5/5           26s        10m\n</code></pre> <pre><code>kubectl delete jobs.batch sleepy\n</code></pre> </li> <li> <p>Edit <code>job.yaml</code> and add <code>parallelism: 2</code> so two pods run at the same time.</p> <pre><code>vim job.yaml\n</code></pre> <pre><code>spec:\n  completions: 5\n  parallelism: 2        # &lt;-- add this line\n  template:\n</code></pre> <p>Tip</p> <p>Pre-built version available: <code>kubectl create -f job-v3.yaml</code></p> </li> <li> <p>Create the job and verify two pods run simultaneously.</p> <pre><code>kubectl create -f job.yaml\nkubectl get pods\n</code></pre> <pre><code>NAME             READY   STATUS    RESTARTS   AGE\nsleepy-8xwpc     1/1     Running   0          5s\nsleepy-xjqnf     1/1     Running   0          5s\n...\n</code></pre> </li> <li> <p>Edit <code>job.yaml</code> again and add <code>activeDeadlineSeconds: 15</code> to forcibly stop the job after 15 seconds.</p> <pre><code>vim job.yaml\n</code></pre> <pre><code>spec:\n  completions: 5\n  parallelism: 2\n  activeDeadlineSeconds: 15   # &lt;-- add this line\n  template:\n</code></pre> <p>Tip</p> <p>Pre-built version available: <code>kubectl create -f job-v4.yaml</code></p> </li> <li> <p>Delete and recreate the job. It will stop after 15 seconds regardless of how many completions remain.</p> <pre><code>kubectl delete jobs.batch sleepy\nkubectl create -f job.yaml\n</code></pre> <p>Wait ~20 seconds, then verify the job stopped early.</p> <pre><code>kubectl get jobs\n</code></pre> <pre><code>NAME     COMPLETIONS   DURATION   AGE\nsleepy   4/5           16s        16s\n</code></pre> </li> <li> <p>Check the status message in the job YAML - it should show <code>DeadlineExceeded</code>.</p> <pre><code>kubectl get job sleepy -o yaml | grep -A8 \"^status:\"\n</code></pre> <pre><code>status:\n  conditions:\n  - message: Job was active longer than specified deadline\n    reason: DeadlineExceeded\n    status: \"True\"\n    type: Failed\n  succeeded: 4\n</code></pre> </li> <li> <p>Delete the job.</p> <pre><code>kubectl delete jobs.batch sleepy\n</code></pre> </li> </ol>"},{"location":"ch04_labs/#exercise-43-create-a-cronjob","title":"Exercise 4.3: Create a CronJob","text":"<p>A CronJob creates Jobs on a recurring schedule using standard Linux cron syntax.</p> <ol> <li> <p>Review the provided <code>cronjob.yaml</code> file. It runs the sleep container every 2 minutes.</p> <pre><code>cat cronjob.yaml\n</code></pre> <pre><code>apiVersion: batch/v1\nkind: CronJob\nmetadata:\n  name: sleepy\nspec:\n  schedule: \"*/2 * * * *\"\n  jobTemplate:\n    spec:\n      template:\n        spec:\n          containers:\n          - name: resting\n            image: busybox\n            command: [\"/bin/sleep\"]\n            args: [\"3\"]\n          restartPolicy: Never\n</code></pre> <p>batch/v1beta1 is removed</p> <p>A file named <code>cron-job.yaml</code> is also in your directory - it uses <code>apiVersion: batch/v1beta1</code> which was removed in Kubernetes 1.25. Do not use it. Always use <code>batch/v1</code> for CronJobs on this cluster.</p> </li> <li> <p>Create the CronJob and monitor it. The first job will not run for up to 2 minutes.</p> <pre><code>kubectl create -f cronjob.yaml\n</code></pre> <pre><code>cronjob.batch/sleepy created\n</code></pre> <pre><code>kubectl get cronjobs.batch\n</code></pre> <pre><code>NAME     SCHEDULE      SUSPEND   ACTIVE   LAST SCHEDULE   AGE\nsleepy   */2 * * * *   False     0        &lt;none&gt;          8s\n</code></pre> </li> <li> <p>After 2 minutes, verify a Job was created by the CronJob.</p> <pre><code>kubectl get cronjobs.batch\n</code></pre> <pre><code>NAME     SCHEDULE      SUSPEND   ACTIVE   LAST SCHEDULE   AGE\nsleepy   */2 * * * *   False     0        21s             2m1s\n</code></pre> <pre><code>kubectl get jobs.batch\n</code></pre> <pre><code>NAME                COMPLETIONS   DURATION   AGE\nsleepy-1539722040   1/1           5s         18s\n</code></pre> <p>Let it run for a few more cycles and you will see multiple completed Jobs listed.</p> </li> <li> <p>Edit <code>cronjob.yaml</code> to make the sleep command run for 30 seconds and add <code>activeDeadlineSeconds: 10</code> inside the job template, so that each job is forcibly killed after 10 seconds.</p> <pre><code>vim cronjob.yaml\n</code></pre> <p>Modify the <code>jobTemplate.spec.template.spec</code> section:</p> <pre><code>jobTemplate:\n  spec:\n    template:\n      spec:\n        activeDeadlineSeconds: 10   # &lt;-- add this line\n        containers:\n        - name: resting\n          image: busybox\n          command: [\"/bin/sleep\"]\n          args: [\"30\"]              # &lt;-- change from \"3\" to \"30\"\n        restartPolicy: Never\n</code></pre> </li> <li> <p>Delete and recreate the CronJob. Wait 2 minutes, then observe that jobs are created but never complete - they are killed by the deadline.</p> <pre><code>kubectl delete cronjobs.batch sleepy\nkubectl create -f cronjob.yaml\n</code></pre> <p>Tip</p> <p>Pre-built version available: <code>kubectl create -f cronjob-v2.yaml</code></p> <p>Wait ~2 minutes, then check:</p> <pre><code>kubectl get jobs\n</code></pre> <pre><code>NAME                COMPLETIONS   DURATION   AGE\nsleepy-1539723240   0/1           61s        61s\n</code></pre> </li> <li> <p>Delete the CronJob and all its associated jobs.</p> <pre><code>kubectl delete cronjobs.batch sleepy\n</code></pre> <pre><code>cronjob.batch \"sleepy\" deleted\n</code></pre> </li> </ol>"},{"location":"ch04_labs/#exercise-44-using-labels","title":"Exercise 4.4: Using Labels","text":"<p>Labels are key-value pairs attached to objects. Selectors use them to filter and manage groups of objects.</p> <ol> <li> <p>Create a new <code>design2</code> deployment.</p> <pre><code>kubectl create deployment design2 --image=nginx\n</code></pre> <pre><code>deployment.apps/design2 created\n</code></pre> </li> <li> <p>View the deployment with <code>-o wide</code> and note the <code>SELECTOR</code> column.</p> <pre><code>kubectl get deployments.apps design2 -o wide\n</code></pre> <pre><code>NAME      READY   UP-TO-DATE   AVAILABLE   ...   SELECTOR\ndesign2   1/1     1            1           ...   app=design2\n</code></pre> </li> <li> <p>Use the <code>-l</code> option to list only pods matching the deployment selector.</p> <pre><code>kubectl get -l app=design2 pod\n</code></pre> <pre><code>NAME                       READY   STATUS    RESTARTS   AGE\ndesign2-766d48574f-5w274   1/1     Running   0          3m\n</code></pre> </li> <li> <p>View the pod labels in full YAML output using <code>--selector</code>.</p> <pre><code>kubectl get --selector app=design2 pod -o yaml | grep -A4 \"labels:\"\n</code></pre> <p>You should see both <code>app: design2</code> and <code>pod-template-hash</code> labels.</p> </li> <li> <p>Edit the pod's <code>app</code> label and change it to your favourite colour (e.g. <code>orange</code>).</p> <pre><code>kubectl edit pod design2-766d48574f-5w274\n</code></pre> <p>Find and change:</p> <pre><code>labels:\n  app: orange    # &lt;-- change from design2\n  pod-template-hash: 766d48574f\n</code></pre> </li> <li> <p>Immediately check how many pods the deployment now manages.</p> <pre><code>kubectl get pods | grep design2\n</code></pre> <pre><code>design2-766d48574f-5w274   1/1   Running   0   82s\ndesign2-766d48574f-xttgg   1/1   Running   0   2m12s\n</code></pre> <p>There are now two pods. The deployment noticed the edited pod no longer matched its selector (<code>app=design2</code>) and created a replacement. The edited pod is now orphaned - no controller manages it.</p> </li> <li> <p>Delete the deployment.</p> <pre><code>kubectl delete deploy design2\n</code></pre> <pre><code>deployment.apps \"design2\" deleted\n</code></pre> </li> <li> <p>Check for remaining pods. The orphaned pod (with the colour label) still exists - the deployment deletion only removed pods it was managing.</p> <pre><code>kubectl get pods | grep design2\n</code></pre> <pre><code>design2-766d48574f-5w274   1/1   Running   0   38m\n</code></pre> </li> <li> <p>Delete the orphaned pod using the colour label you assigned.</p> <pre><code>kubectl delete pod -l app=orange\n</code></pre> <pre><code>pod \"design2-766d48574f-5w274\" deleted\n</code></pre> </li> </ol>"},{"location":"ch04_labs/#exercise-45-setting-pod-resource-limits","title":"Exercise 4.5: Setting Pod Resource Limits","text":"<p>Resource requests tell the scheduler how much CPU/memory a pod needs. Limits cap what it can use.</p> <ol> <li> <p>Review and create the <code>stress.yaml</code> deployment. It runs the <code>vish/stress</code> image which deliberately consumes CPU and memory.</p> <pre><code>cat stress.yaml\nkubectl create -f stress.yaml\n</code></pre> </li> <li> <p>On the controller, run <code>top</code> to see the stress process consuming CPU. Use <code>q</code> to exit.</p> <pre><code>top\n</code></pre> <p>Then open a separate terminal on worker1 and run the same:</p> <pre><code>top\n</code></pre> </li> <li> <p>Delete the deployment, then edit <code>stress.yaml</code> to add resource limits and requests.</p> <pre><code>kubectl delete -f stress.yaml\nvim stress.yaml\n</code></pre> <p>Add the <code>resources</code> block under the container name:</p> <pre><code>containers:\n- image: vish/stress\n  name: stressmeout\n  resources:\n    limits:\n      cpu: \"1\"\n      memory: \"1Gi\"\n    requests:\n      cpu: \"0.5\"\n      memory: \"500Mi\"\n  args:\n  - -cpus\n  - \"2\"\n  - -mem-total\n  - \"1950Mi\"\n</code></pre> <p>Tip</p> <p>Pre-built version available: <code>kubectl create -f stress-v2.yaml</code></p> </li> <li> <p>Create the deployment and check the pod status. Because the stress command requests more memory than the limit, the container will be OOMKilled.</p> <pre><code>kubectl create -f stress.yaml\nkubectl get pod\n</code></pre> <pre><code>NAME                           READY   STATUS      RESTARTS   AGE\nstressmeout-7fbbbcc887-v9kvb   0/1     OOMKilled   2          32s\n</code></pre> </li> <li> <p>Delete the deployment and edit <code>stress.yaml</code> again. This time:</p> <ul> <li>Increase the memory limit to <code>2Gi</code> (enough for the stress workload)</li> <li>Uncomment and set the <code>nodeSelector</code> to pin the pod to worker1 (to protect the control plane)</li> </ul> <pre><code>kubectl delete -f stress.yaml\nvim stress.yaml\n</code></pre> <pre><code>spec:\n  nodeSelector:\n    kubernetes.io/hostname: worker1    # &lt;-- uncomment and set to worker1\n  containers:\n...\n  resources:\n    limits:\n      cpu: \"2\"\n      memory: \"2Gi\"\n    requests:\n      cpu: \"0.5\"\n      memory: \"500Mi\"\n</code></pre> <p>Tip</p> <p>Pre-built version available: <code>kubectl create -f stress-v3.yaml</code></p> </li> <li> <p>Create the deployment and verify the pod runs on <code>worker1</code> without OOMKilling.</p> <pre><code>kubectl create -f stress.yaml\nkubectl get pod -o wide\n</code></pre> <pre><code>NAME                           READY   STATUS    NODE\nstressmeout-...                1/1     Running   worker1\n</code></pre> </li> <li> <p>Use <code>kubectl describe node worker1</code> to view how much of worker1's CPU and memory is now being consumed.</p> <pre><code>kubectl describe node worker1 | grep -A8 \"Allocated resources\"\n</code></pre> </li> <li> <p>Try editing the limits to values higher than the node has available. Observe how the scheduler handles an unschedulable pod.</p> </li> <li> <p>Delete the deployment when done.</p> <pre><code>kubectl delete deploy stressmeout\n</code></pre> </li> </ol>"},{"location":"ch04_labs/#exercise-46-simple-initcontainer","title":"Exercise 4.6: Simple initContainer","text":"<p>An <code>initContainer</code> runs to completion before the main container starts. If it fails, the pod never becomes ready.</p> <ol> <li> <p>Review the provided <code>init-tester.yaml</code>. The initContainer runs <code>/bin/false</code> which always exits with a non-zero code.</p> <pre><code>cat init-tester.yaml\n</code></pre> <pre><code>apiVersion: v1\nkind: Pod\nmetadata:\n  name: init-tester\n  labels:\n    app: inittest\nspec:\n  containers:\n  - name: webservice\n    image: nginx\n  initContainers:\n  - name: failed\n    image: busybox\n    command: [/bin/false]\n</code></pre> </li> <li> <p>Create the pod and observe the failure.</p> <pre><code>kubectl create -f init-tester.yaml\nkubectl get pod init-tester\n</code></pre> <pre><code>NAME           READY   STATUS       RESTARTS   AGE\ninit-tester    0/1     Init:Error   2          30s\n</code></pre> <p>Use <code>describe</code> to read the init container failure events.</p> <pre><code>kubectl describe pod init-tester\n</code></pre> </li> <li> <p>Delete the pod, fix the command from <code>/bin/false</code> to <code>/bin/true</code>, and recreate it.</p> <pre><code>kubectl delete pod init-tester\nvim init-tester.yaml\n</code></pre> <p>Change:</p> <pre><code>command: [/bin/true]\n</code></pre> <p>Tip</p> <p>Pre-built version available: <code>kubectl create -f init-tester-v2.yaml</code></p> </li> <li> <p>Create the pod again and verify the nginx webservice container starts once the initContainer succeeds.</p> <pre><code>kubectl create -f init-tester.yaml\nkubectl get pod init-tester\n</code></pre> <pre><code>NAME           READY   STATUS    RESTARTS   AGE\ninit-tester    1/1     Running   0          10s\n</code></pre> </li> <li> <p>Delete the pod when done.</p> <pre><code>kubectl delete pod init-tester\n</code></pre> </li> </ol>"},{"location":"ch04_labs/#exercise-47-exploring-custom-resource-definitions","title":"Exercise 4.7: Exploring Custom Resource Definitions","text":"<p>CRDs extend the Kubernetes API with new resource types.</p> <ol> <li> <p>View all CRDs currently installed in the cluster.</p> <pre><code>kubectl get crd\n</code></pre> <p>With Calico as the CNI, you will see Calico-related CRDs (e.g. <code>caliconetworkpolicies</code>, <code>ippools</code>, <code>felixconfigurations</code>). If additional operators are installed you may see more.</p> </li> <li> <p>Pick one of the CRDs listed and inspect its full definition.</p> <pre><code>kubectl get crd &lt;crd-name&gt; -o yaml\n</code></pre> <p>Note the <code>group</code>, <code>kind</code>, <code>plural</code>, <code>scope</code>, and available <code>versions</code>.</p> </li> <li> <p>Try querying for resources of that custom type. Most will return empty unless instances have been created.</p> <pre><code>kubectl get &lt;kind-from-crd&gt;\n</code></pre> <pre><code>No resources found\n</code></pre> </li> <li> <p>Explore additional CRDs in the cluster. Note that Kubernetes itself ships with several built-in CRDs for features like autoscaling and flow control.</p> <pre><code>kubectl get crd | wc -l\n</code></pre> </li> </ol>"},{"location":"ch04_labs/#exercise-48-domain-review","title":"Exercise 4.8: Domain Review","text":"<p>Important</p> <p>Source pages and content in this review can change at any time. Always check the current version before your exam.</p> <p>Revisit the CKAD curriculum and locate topics covered in this chapter:</p> <ul> <li>Understand Jobs and CronJobs</li> <li>Understanding and defining resource requirements, limits and quotas</li> <li>Understand multi-container Pod design patterns (sidecar, init, and others)</li> <li> <p>Discover and use resources that extend Kubernetes (CRD)</p> </li> <li> <p>Create a pod using <code>design-review1.yaml</code>. Examine the resource limits and requests.</p> <pre><code>kubectl create -f design-review1.yaml\n</code></pre> </li> <li> <p>Determine the CPU and memory resource requirements of <code>design-pod1</code>.</p> </li> <li>Edit the pod resource requirements such that the CPU limit is exactly twice the amount requested. (Hint: current limit is <code>2.22</code>, request is <code>0.3</code> - subtract <code>0.22</code> from the limit to make it exactly <code>2x</code> the request.)</li> <li> <p>Increase the memory limit until the pod achieves <code>Running</code> status and holds it for at least a minute. Find the minimum memory limit required.</p> </li> <li> <p>Create the pods defined in <code>design-review2.yaml</code>.</p> <pre><code>kubectl create -f design-review2.yaml\n</code></pre> <p>View all pods and their labels.</p> <pre><code>kubectl get pods --show-labels\n</code></pre> </li> <li> <p>Delete only the pods with the label <code>review=tux</code> using <code>--selector</code>. This should delete two of the four pods.</p> <pre><code>kubectl delete pod --selector review=tux\n</code></pre> </li> <li> <p>Create a new CronJob that runs <code>busybox</code> with the <code>sleep 30</code> command every 3 minutes. Verify it runs correctly. Then update the schedule so it runs at a specific time 10 minutes from now, every week (e.g. if it's 14:14, set it to run at <code>24 14 * * 1</code>).</p> </li> <li> <p>Delete all objects created during this review.</p> <pre><code>kubectl delete pod design-pod1 label-pod3 label-pod4 --ignore-not-found\nkubectl delete cronjobs.batch sleepy --ignore-not-found\nkubectl delete deploy stressmeout --ignore-not-found\n</code></pre> </li> </ul>"},{"location":"ch05_labs/","title":"Chapter 5 - Deployment Configuration","text":""},{"location":"ch05_labs/#lab-overview","title":"Lab Overview","text":"<p>This chapter builds directly on Chapter 3. You will configure your existing <code>simpleapp</code> deployment with ConfigMaps (as environment variables and volume mounts), attach NFS-backed persistent storage, configure the fluentd logging sidecar with a ConfigMap, and practice rolling updates and rollbacks.</p> <p>Prerequisites</p> <p>This chapter requires a working <code>~/app1/simpleapp.yaml</code> from Chapter 3, along with the local registry running (<code>try1</code> deployment with 6 replicas). If you need to rebuild it, revisit Chapter 3 before continuing.</p> <p>Save a backup of your simpleapp.yaml before starting:</p> <pre><code>cp ~/app1/simpleapp.yaml ~/beforeLab5.yaml\n</code></pre>"},{"location":"ch05_labs/#exercise-51-configure-the-deployment-configmaps","title":"Exercise 5.1: Configure the Deployment - ConfigMaps","text":"<p>ConfigMaps decouple configuration from container images. They can be created from literal values, files, or directories, and consumed as environment variables or volume mounts.</p> <ol> <li> <p>Connect to the controller node and move into the chapter lab directory.</p> <pre><code>cd ~/lfd459/ch05-deployment-config\nls\n</code></pre> </li> <li> <p>Create a directory of files to ingest into a ConfigMap, and a separate single file.</p> <pre><code>mkdir primary\necho c &gt; primary/cyan\necho m &gt; primary/magenta\necho y &gt; primary/yellow\necho k &gt; primary/black\necho \"known as key\" &gt;&gt; primary/black\necho blue &gt; favorite\n</code></pre> </li> <li> <p>Create a ConfigMap called <code>colors</code> using all three ingestion methods at once: a literal, a single file, and a directory.</p> <pre><code>kubectl create configmap colors \\\n--from-literal=text=black \\\n--from-file=./favorite \\\n--from-file=./primary/\n</code></pre> <pre><code>configmap/colors created\n</code></pre> </li> <li> <p>View the ConfigMap and verify all six keys are present.</p> <pre><code>kubectl get configmap colors -o yaml\n</code></pre> <p>You should see keys: <code>black</code>, <code>cyan</code>, <code>favorite</code>, <code>magenta</code>, <code>text</code>, <code>yellow</code>.</p> </li> <li> <p>Edit <code>simpleapp.yaml</code> (located at <code>~/lfd459/ch03-build/app1/simpleapp.yaml</code>) to add a single environment variable <code>ilike</code> sourced from the <code>colors</code> ConfigMap. Add the <code>env:</code> block inside the <code>simpleapp</code> container spec, after <code>imagePullPolicy</code>.</p> <pre><code>vim ~/lfd459/ch03-build/app1/simpleapp.yaml\n</code></pre> <p>Find the simpleapp container section and add:</p> <pre><code>- image: 10.97.40.62:5000/simpleapp\n  imagePullPolicy: Always\n  name: simpleapp\n  env:\n  - name: ilike\n    valueFrom:\n      configMapKeyRef:\n        name: colors\n        key: favorite\n</code></pre> </li> <li> <p>Delete and recreate the deployment.</p> <pre><code>kubectl delete deployment try1\nkubectl create -f ~/lfd459/ch03-build/app1/simpleapp.yaml\n</code></pre> </li> <li> <p>Verify the <code>ilike</code> environment variable is set inside the container.</p> <pre><code>kubectl get pods\n</code></pre> <pre><code>kubectl exec -c simpleapp -it &lt;try1-pod-name&gt; -- /bin/bash -c 'echo $ilike'\n</code></pre> <pre><code>blue\n</code></pre> </li> <li> <p>Edit <code>simpleapp.yaml</code> again to add <code>envFrom</code> so all keys from the <code>colors</code> ConfigMap are injected as environment variables.</p> <pre><code>vim ~/lfd459/ch03-build/app1/simpleapp.yaml\n</code></pre> <p>Add after <code>key: favorite</code>:</p> <pre><code>      key: favorite\n  envFrom:\n  - configMapRef:\n      name: colors\n  imagePullPolicy: Always\n</code></pre> </li> <li> <p>Delete, recreate, and verify all color keys are visible as environment variables.</p> <pre><code>kubectl delete deployment try1\nkubectl create -f ~/lfd459/ch03-build/app1/simpleapp.yaml\nkubectl exec -it &lt;try1-pod-name&gt; -- /bin/bash -c 'env' | grep -E 'ilike|cyan|magenta|yellow|black|text'\n</code></pre> </li> <li> <p>Create the <code>fast-car</code> ConfigMap from the provided YAML file.</p> <pre><code>kubectl create -f car-map.yaml\n</code></pre> <pre><code>configmap/fast-car created\n</code></pre> <pre><code>kubectl get configmap fast-car -o yaml\n</code></pre> </li> <li> <p>Edit <code>simpleapp.yaml</code> to mount the <code>fast-car</code> ConfigMap as a volume at <code>/etc/cars</code> inside the <code>simpleapp</code> container.</p> <pre><code>vim ~/lfd459/ch03-build/app1/simpleapp.yaml\n</code></pre> <p>In the <code>simpleapp</code> container spec, add before <code>env:</code>:</p> <pre><code>    volumeMounts:\n    - mountPath: /etc/cars\n      name: car-vol\n</code></pre> <p>At the end of the pod <code>spec</code> (after all containers, before <code>status:</code>), add:</p> <pre><code>  volumes:\n  - name: car-vol\n    configMap:\n      defaultMode: 420\n      name: fast-car\n</code></pre> <p>Tip</p> <p>Pre-built versions are available: <code>simpleapp-v2.yaml</code> through <code>simpleapp-v6.yaml</code> in <code>~/lfd459/ch05-deployment-config/</code>. Each version corresponds to the state of <code>simpleapp.yaml</code> after each major edit in this chapter.</p> </li> <li> <p>Delete and recreate the deployment.</p> <pre><code>kubectl delete deployment try1\nkubectl create -f ~/lfd459/ch03-build/app1/simpleapp.yaml\n</code></pre> </li> <li> <p>The deployment will show <code>0/6</code> ready because the <code>readinessProbe</code> is still checking for <code>/tmp/healthy</code>. Update the probe to check for <code>/etc/cars</code> instead.</p> <pre><code>kubectl delete deployment try1\nvim ~/lfd459/ch03-build/app1/simpleapp.yaml\n</code></pre> <p>Change the <code>readinessProbe</code> exec command inside the <code>simpleapp</code> container:</p> <pre><code>    readinessProbe:\n      exec:\n        command:\n        - ls\n        - /etc/cars\n      periodSeconds: 5\n</code></pre> <pre><code>kubectl create -f ~/lfd459/ch03-build/app1/simpleapp.yaml\n</code></pre> </li> <li> <p>Wait about a minute. All six pods should show <code>2/2 Running</code>.</p> <pre><code>kubectl get pods\n</code></pre> <pre><code>NAME                         READY   STATUS    RESTARTS   AGE\ntry1-7865dcb948-2dzc8        2/2     Running   0          1m\ntry1-7865dcb948-7fkh7        2/2     Running   0          1m\n...\n</code></pre> </li> <li> <p>Verify the ConfigMap data is visible inside the container as a file.</p> <pre><code>kubectl exec -c simpleapp -it &lt;try1-pod-name&gt; -- /bin/bash -c 'cat /etc/cars/car.trim'\n</code></pre> <pre><code>Shelby\n</code></pre> </li> </ol>"},{"location":"ch05_labs/#exercise-52-configure-the-deployment-attaching-storage","title":"Exercise 5.2: Configure the Deployment - Attaching Storage","text":"<p>We will configure an NFS server on the controller node, create a PersistentVolume backed by it, and attach it to the <code>try1</code> deployment.</p> <ol> <li> <p>Run the <code>CreateNFS.sh</code> script to install and configure NFS on the controller node.</p> <pre><code>bash CreateNFS.sh\n</code></pre> <p>The script installs <code>nfs-kernel-server</code>, creates <code>/opt/sfw/</code>, exports it, and creates <code>/opt/sfw/hello.txt</code>. At the end you should see:</p> <pre><code>Should be ready. Test here and second node\nExport list for localhost:\n/opt/sfw *\n</code></pre> </li> <li> <p>Connect to worker1 and verify the NFS export is visible from the worker.</p> <pre><code>sudo apt-get -y install nfs-common nfs-kernel-server\nshowmount -e controller\n</code></pre> <pre><code>Export list for controller:\n/opt/sfw *\n</code></pre> <p>Repeat on worker2.</p> </li> <li> <p>Test the mount from worker1.</p> <pre><code>sudo mount controller:/opt/sfw /mnt\nls -l /mnt\n</code></pre> <pre><code>total 4\n-rw-r--r-- 1 root root 9 ... hello.txt\n</code></pre> <pre><code>sudo umount /mnt\n</code></pre> </li> <li> <p>Return to the controller. Verify <code>PVol.yaml</code> has <code>server: controller</code>.</p> <pre><code>cat PVol.yaml\n</code></pre> </li> <li> <p>Create the PersistentVolume and verify it shows <code>Available</code>.</p> <pre><code>kubectl create -f PVol.yaml\n</code></pre> <pre><code>persistentvolume/pvvol-1 created\n</code></pre> <pre><code>kubectl get pv\n</code></pre> <pre><code>NAME       CAPACITY   ACCESS MODES   RECLAIM POLICY   STATUS\npvvol-1    1Gi        RWX            Retain           Available\n...\n</code></pre> </li> <li> <p>Check any existing PVCs (from the registry deployment in Chapter 3).</p> <pre><code>kubectl get pvc\n</code></pre> </li> <li> <p>Create a PVC using <code>pvc.yaml</code>.</p> <pre><code>kubectl create -f pvc.yaml\n</code></pre> <pre><code>persistentvolumeclaim/pvc-one created\n</code></pre> <pre><code>kubectl get pvc pvc-one\n</code></pre> <pre><code>NAME      STATUS   VOLUME    CAPACITY   ACCESS MODES\npvc-one   Bound    pvvol-1   1Gi        RWX\n</code></pre> <p>Note the PVC claimed 1Gi even though only 200Mi was requested - Kubernetes uses the smallest available volume that satisfies the request.</p> </li> <li> <p>Verify <code>pvvol-1</code> is now <code>Bound</code>.</p> <pre><code>kubectl get pv pvvol-1\n</code></pre> <pre><code>NAME      CAPACITY   ACCESS MODES   STATUS   CLAIM\npvvol-1   1Gi        RWX            Bound    default/pvc-one\n</code></pre> </li> <li> <p>Edit <code>~/app1/simpleapp.yaml</code> to add the NFS volume to the <code>simpleapp</code> container.</p> <pre><code>vim ~/lfd459/ch03-build/app1/simpleapp.yaml\n</code></pre> <p>In the simpleapp <code>volumeMounts</code> section, add after the <code>car-vol</code> mount:</p> <pre><code>    - name: nfs-vol\n      mountPath: /opt\n</code></pre> <p>In the <code>volumes</code> section, add after the <code>car-vol</code> volume:</p> <pre><code>  - name: nfs-vol\n    persistentVolumeClaim:\n      claimName: pvc-one\n</code></pre> </li> <li> <p>Delete and recreate the deployment.</p> <pre><code>kubectl delete deployment try1\nkubectl create -f ~/lfd459/ch03-build/app1/simpleapp.yaml\n</code></pre> </li> <li> <p>Verify the NFS volume is mounted under <code>/opt</code> in a pod.</p> <pre><code>kubectl describe pod &lt;try1-pod-name&gt; | grep -A5 \"Mounts:\"\n</code></pre> <pre><code>Mounts:\n  /etc/cars from car-vol (rw)\n  /opt from nfs-vol (rw)\n  ...\n</code></pre> </li> </ol>"},{"location":"ch05_labs/#exercise-53-using-configmaps-to-configure-containers","title":"Exercise 5.3: Using ConfigMaps to Configure Containers","text":"<p>Now we return to the <code>basicpod</code> from Chapter 2 and fully configure the fluentd logging sidecar using a ConfigMap and a shared PersistentVolume.</p> <ol> <li> <p>We will work with a copy of <code>basic.yaml</code> in the chapter directory. Copy it now if not already present.</p> <pre><code>cp ~/lfd459/ch02-architecture/basic.yaml .\ncat basic.yaml\n</code></pre> <p>It should have two containers: <code>webcont</code> (nginx) and <code>fdlogger</code> (fluentd).</p> </li> <li> <p>Create the directory that will back the hostPath PV.</p> <pre><code>sudo mkdir /tmp/weblog\n</code></pre> </li> <li> <p>Create the PV using <code>weblog-pv.yaml</code>.</p> <pre><code>kubectl create -f weblog-pv.yaml\n</code></pre> <pre><code>persistentvolume/weblog-pv-volume created\n</code></pre> <pre><code>kubectl get pv weblog-pv-volume\n</code></pre> <pre><code>NAME               CAPACITY   ACCESS MODES   STATUS\nweblog-pv-volume   100Mi      RWO            Available\n</code></pre> </li> <li> <p>Create the PVC using <code>weblog-pvc.yaml</code> and verify it binds to <code>weblog-pv-volume</code>.</p> <pre><code>kubectl create -f weblog-pvc.yaml\n</code></pre> <pre><code>persistentvolumeclaim/weblog-pv-claim created\n</code></pre> <pre><code>kubectl get pvc weblog-pv-claim\n</code></pre> <pre><code>NAME             STATUS   VOLUME             CAPACITY   STORAGE CLASS\nweblog-pv-claim  Bound    weblog-pv-volume   100Mi      manual\n</code></pre> </li> <li> <p>Edit <code>basic.yaml</code> to add the shared volume to both containers.</p> <pre><code>vim basic.yaml\n</code></pre> <p>The final file should look like <code>basic.yaml-with-edits</code>. A pre-built version is available as <code>basic-v2.yaml</code> in <code>~/lfd459/ch05-deployment-config/</code>.</p> <pre><code>apiVersion: v1\nkind: Pod\nmetadata:\n  name: basicpod\n  labels:\n    type: webserver\nspec:\n  volumes:\n  - name: weblog-pv-storage\n    persistentVolumeClaim:\n      claimName: weblog-pv-claim\n  containers:\n  - name: webcont\n    image: nginx\n    ports:\n    - containerPort: 80\n    volumeMounts:\n    - mountPath: \"/var/log/nginx/\"\n      name: weblog-pv-storage\n  - name: fdlogger\n    image: fluent/fluentd:v1.16\n    volumeMounts:\n    - mountPath: \"/var/log\"\n      name: weblog-pv-storage\n</code></pre> </li> <li> <p>Create the pod and open a shell into the <code>webcont</code> container. Verify the nginx access log is a real file and start tailing it.</p> <pre><code>kubectl create -f basic.yaml\n</code></pre> <pre><code>pod/basicpod created\n</code></pre> <pre><code>kubectl exec -c webcont -it basicpod -- /bin/bash\n</code></pre> <pre><code>root@basicpod:/# ls -l /var/log/nginx/access.log\n-rw-r--r-- 1 root root 0 ... /var/log/nginx/access.log\n\nroot@basicpod:/# tail -f /var/log/nginx/access.log\n</code></pre> </li> <li> <p>Open a second terminal on the controller. Get the pod IP and curl the nginx default page.</p> <pre><code>kubectl get pods -o wide\n</code></pre> <pre><code>NAME       READY   STATUS    IP               NODE\nbasicpod   2/2     Running   10.244.1.23      worker1\n</code></pre> <pre><code>curl http://10.244.1.23\n</code></pre> <p>You should see a new log entry appear in the tail window in your first terminal. Use <code>Ctrl-C</code> and <code>exit</code> to return to the host.</p> </li> <li> <p>Create the fluentd ConfigMap from <code>weblog-configmap.yaml</code>.</p> <pre><code>kubectl create -f weblog-configmap.yaml\n</code></pre> <pre><code>configmap/fluentd-config created\n</code></pre> </li> <li> <p>Check the current logs for both containers.</p> <pre><code>kubectl logs basicpod webcont\nkubectl logs basicpod fdlogger\n</code></pre> </li> <li> <p>Edit <code>basic.yaml</code> to configure the <code>fdlogger</code> container to use the ConfigMap.</p> <pre><code>vim basic.yaml\n</code></pre> <p>In the <code>volumes</code> section, add after <code>weblog-pv-storage</code>:</p> <pre><code>  - name: log-config\n    configMap:\n      name: fluentd-config\n</code></pre> <p>In the <code>fdlogger</code> container, add <code>env</code> and a second <code>volumeMounts</code> entry:</p> <p>Tip</p> <p>Pre-built version available: <code>basic-v3.yaml</code> in <code>~/lfd459/ch05-deployment-config/</code></p> <pre><code>  - name: fdlogger\n    image: fluent/fluentd:v1.16\n    env:\n    - name: FLUENTD_OPT\n      value: -c /fluentd/etc/fluent.conf\n    volumeMounts:\n    - mountPath: \"/var/log\"\n      name: weblog-pv-storage\n    - name: log-config\n      mountPath: \"/fluentd/etc\"\n</code></pre> </li> <li> <p>Delete and recreate the pod.</p> <pre><code>kubectl delete pod basicpod\nkubectl create -f basic.yaml\nkubectl get pod basicpod -o wide\n</code></pre> <pre><code>NAME       READY   STATUS    IP\nbasicpod   2/2     Running   10.244.1.xx\n</code></pre> </li> <li> <p>Curl the nginx page a few times, then check the <code>fdlogger</code> logs.</p> <pre><code>curl http://&lt;basicpod-ip&gt;\ncurl http://&lt;basicpod-ip&gt;\nkubectl logs basicpod fdlogger | tail -10\n</code></pre> <p>Look for lines like:</p> <pre><code>count.format1: {\"message\":\"10.244.x.x - - [date] \\\"GET / HTTP/1.1\\\" 200 612 ...\"}\n</code></pre> </li> </ol>"},{"location":"ch05_labs/#exercise-54-rolling-updates-and-rollbacks","title":"Exercise 5.4: Rolling Updates and Rollbacks","text":"<ol> <li> <p>Move into the <code>app1</code> directory and add a comment to <code>simple.py</code> to produce a slightly different image.</p> <pre><code>cd ~/lfd459/ch03-build/app1\nvim simple.py\n</code></pre> <p>Add a comment on the last line:</p> <pre><code>## Sleep for five seconds then continue the loop\ntime.sleep(5)\n## Adding a new comment so image is different.\n</code></pre> </li> <li> <p>Check the current images.</p> <pre><code>sudo podman images | grep simple\n</code></pre> </li> <li> <p>Build the updated image.</p> <pre><code>sudo podman build -t simpleapp .\n</code></pre> </li> <li> <p>Tag and push the updated image as <code>v2</code>.</p> <pre><code>sudo podman tag simpleapp $repo/simpleapp:v2\nsudo podman push $repo/simpleapp:v2\n</code></pre> </li> <li> <p>Verify the registry now has both <code>latest</code> and <code>v2</code> tagged images.</p> <pre><code>curl $repo/v2/simpleapp/tags/list\n</code></pre> <pre><code>{\"name\":\"simpleapp\",\"tags\":[\"latest\",\"v2\"]}\n</code></pre> </li> <li> <p>Connect to worker1 and pull both versions to confirm the worker can access them.</p> <pre><code>sudo podman pull $repo/simpleapp\nsudo podman pull $repo/simpleapp:v2\n</code></pre> </li> <li> <p>Return to the controller. Use <code>kubectl edit</code> to update the <code>try1</code> deployment to use <code>v2</code>.</p> <pre><code>kubectl edit deployment try1\n</code></pre> <p>Change:</p> <pre><code>- image: 10.97.40.62:5000/simpleapp\n</code></pre> <p>To:</p> <pre><code>- image: 10.97.40.62:5000/simpleapp:v2\n</code></pre> </li> <li> <p>Watch the rolling update.</p> <pre><code>kubectl get events | tail -20\nkubectl get pods\n</code></pre> </li> <li> <p>Verify the new pods are using <code>v2</code>.</p> <pre><code>kubectl describe pod &lt;try1-pod-name&gt; | grep Image:\n</code></pre> <pre><code>Image:    10.97.40.62:5000/simpleapp:v2\nImage:    registry.k8s.io/goproxy:0.1\n</code></pre> </li> <li> <p>View the rollout history.</p> <pre><code>kubectl rollout history deployment try1\n</code></pre> <pre><code>REVISION   CHANGE-CAUSE\n1          &lt;none&gt;\n2          &lt;none&gt;\n</code></pre> </li> <li> <p>Compare the two revisions by saving them to files and diffing.</p> <pre><code>kubectl rollout history deployment try1 --revision=1 &gt; one.out\nkubectl rollout history deployment try1 --revision=2 &gt; two.out\ndiff one.out two.out\n</code></pre> </li> <li> <p>Preview the rollback without applying it.</p> <pre><code>kubectl rollout undo --dry-run=client deployment/try1\n</code></pre> </li> <li> <p>Roll back to revision 1.</p> <pre><code>kubectl rollout undo deployment try1 --to-revision=1\n</code></pre> <pre><code>deployment.apps/try1 rolled back\n</code></pre> </li> <li> <p>Wait for all pods to cycle and verify they are back to the original image.</p> <pre><code>kubectl get pods\nkubectl describe pod &lt;try1-pod-name&gt; | grep Image:\n</code></pre> <pre><code>Image:    10.97.40.62:5000/simpleapp\n</code></pre> </li> </ol>"},{"location":"ch05_labs/#exercise-55-domain-review","title":"Exercise 5.5: Domain Review","text":"<p>Important</p> <p>Source pages and content in this review can change at any time. Always check current information before your exam.</p> <p>Revisit the CKAD curriculum for topics covered in this chapter:</p> <ul> <li>Utilize persistent and ephemeral volumes</li> <li>Understand Deployments and how to perform rolling updates</li> <li> <p>Understand ConfigMaps</p> </li> <li> <p>Create a new secret called <code>specialofday</code> using the key <code>entree</code> and the value <code>meatloaf</code>.</p> </li> <li>Create a new deployment called <code>foodie</code> running the <code>nginx</code> image.</li> <li>Add the <code>specialofday</code> secret to the <code>foodie</code> pod, mounted as a volume under <code>/food/</code>.</li> <li>Exec a bash shell into a <code>foodie</code> pod and verify the secret is correctly mounted.</li> <li>Update the <code>foodie</code> deployment to use the <code>nginx:1.28.1-alpine</code> image and verify the new image is in use.</li> <li>Roll back the deployment and verify the standard nginx image is running again.</li> <li>Create a new 200M NFS PersistentVolume called <code>reviewvol</code> using the NFS server configured earlier (<code>controller:/opt/sfw</code>).</li> <li>Create a new PVC called <code>reviewpvc</code> that binds to <code>reviewvol</code>.</li> <li>Edit the <code>foodie</code> deployment to mount the PVC under <code>/newvol</code>.</li> <li>Exec into the nginx container and verify the volume is mounted.</li> <li> <p>Delete all resources created during this review.</p> <pre><code>kubectl delete deployment foodie --ignore-not-found\nkubectl delete secret specialofday --ignore-not-found\nkubectl delete pvc reviewpvc --ignore-not-found\nkubectl delete pv reviewvol --ignore-not-found\n</code></pre> </li> </ul>"},{"location":"ch06_labs/","title":"Chapter 6 - Understanding Security","text":""},{"location":"ch06_labs/#lab-overview","title":"Lab Overview","text":"<p>In this chapter you will work with security contexts to control container UIDs and Linux capabilities, create and consume Secrets, configure ServiceAccounts with RBAC, and understand NetworkPolicies. A new working directory <code>~/app2</code> is used throughout.</p> <p>NetworkPolicy and Calico</p> <p>This cluster uses Calico as the CNI plugin. Calico fully enforces NetworkPolicies - when you apply a policy in Exercises 6.4 and 6.5, traffic will actually be blocked as described. The curl and nc tests will time out as expected. This matches the behaviour you will see on the CKAD exam.</p>"},{"location":"ch06_labs/#exercise-61-set-securitycontext-for-a-pod-and-container","title":"Exercise 6.1: Set SecurityContext for a Pod and Container","text":"<p>A SecurityContext restricts what a container process can do - which UID it runs as, whether it can escalate privileges, and which Linux capabilities it has.</p> <ol> <li> <p>Connect to the controller node. Move into the chapter lab directory. A new working directory <code>~/lfd459/ch06-security</code> is used throughout.</p> <pre><code>cd ~/lfd459/ch06-security\n</code></pre> </li> <li> <p>Review and create the <code>secondapp</code> pod. It sets a pod-level UID of 1000 and a container-level UID of 2000. The container setting takes precedence.</p> <pre><code>cat second.yaml\n</code></pre> <pre><code>apiVersion: v1\nkind: Pod\nmetadata:\n  name: secondapp\nspec:\n  securityContext:\n    runAsUser: 1000\n  containers:\n  - name: busy\n    image: busybox\n    command:\n      - sleep\n      - \"3600\"\n    securityContext:\n      runAsUser: 2000\n      allowPrivilegeEscalation: false\n</code></pre> <pre><code>kubectl create -f second.yaml\n</code></pre> <pre><code>pod/secondapp created\n</code></pre> <pre><code>kubectl get pod secondapp\n</code></pre> <pre><code>NAME         READY   STATUS    RESTARTS   AGE\nsecondapp    1/1     Running   0          21s\n</code></pre> </li> <li> <p>Exec into the container and verify the process is running as UID 2000.</p> <pre><code>kubectl exec -it secondapp -- sh\n</code></pre> <p>Inside the container:</p> <pre><code>/ $ ps aux\nPID   USER     COMMAND\n1     2000     sleep 3600\n8     2000     sh\n</code></pre> </li> <li> <p>Check the current Linux capability bitmask for PID 1.</p> <pre><code>/ $ grep Cap /proc/1/status\nCapBnd: 00000000a80425fb\n/ $ exit\n</code></pre> </li> <li> <p>Decode the capability bitmask using <code>capsh</code> on the controller.</p> <pre><code>capsh --decode=00000000a80425fb\n</code></pre> <p>You should see approximately 14 capabilities including <code>cap_chown</code>, <code>cap_net_bind_service</code>, etc.</p> </li> <li> <p>Delete the pod and edit <code>second.yaml</code> to add two additional Linux capabilities: <code>NET_ADMIN</code> and <code>SYS_TIME</code>.</p> <pre><code>kubectl delete pod secondapp\nvim second.yaml\n</code></pre> <p>Add the <code>capabilities</code> block inside the container's <code>securityContext</code>:</p> <pre><code>    securityContext:\n      runAsUser: 2000\n      allowPrivilegeEscalation: false\n      capabilities:\n        add: [\"NET_ADMIN\", \"SYS_TIME\"]\n</code></pre> <p>Tip</p> <p>Pre-built version available: <code>kubectl create -f second-v2.yaml</code></p> </li> <li> <p>Recreate the pod and check the new capability bitmask.</p> <pre><code>kubectl create -f second.yaml\nkubectl exec -it secondapp -- sh\n</code></pre> <p>Inside:</p> <pre><code>/ $ grep Cap /proc/1/status\nCapBnd: 00000000aa0435fb\n/ $ exit\n</code></pre> </li> <li> <p>Decode the new bitmask. Confirm <code>cap_net_admin</code> and <code>cap_sys_time</code> are now present (16 capabilities total).</p> <pre><code>capsh --decode=00000000aa0435fb\n</code></pre> </li> </ol>"},{"location":"ch06_labs/#exercise-62-create-and-consume-secrets","title":"Exercise 6.2: Create and Consume Secrets","text":"<p>Secrets store sensitive data in base64-encoded form. They are consumed like ConfigMaps - as environment variables or volume mounts.</p> <ol> <li> <p>Generate a base64-encoded password.</p> <pre><code>echo LFTr@1n | base64\n</code></pre> <pre><code>TEZUckAxbgo=\n</code></pre> </li> <li> <p>Review <code>secret.yaml</code> - it already contains the encoded password.</p> <pre><code>cat secret.yaml\n</code></pre> <pre><code>apiVersion: v1\nkind: Secret\nmetadata:\n  name: lfsecret\ndata:\n  password: TEZUckAxbgo=\n</code></pre> </li> <li> <p>Create the secret.</p> <pre><code>kubectl create -f secret.yaml\n</code></pre> <pre><code>secret/lfsecret created\n</code></pre> </li> <li> <p>Edit <code>second.yaml</code> to mount the secret as a volume at <code>/mysqlpassword</code>.</p> <pre><code>vim second.yaml\n</code></pre> <p>In the container spec (after the <code>capabilities</code> block):</p> <pre><code>      volumeMounts:\n      - name: mysql\n        mountPath: /mysqlpassword\n</code></pre> <p>At the pod spec level (same depth as <code>containers:</code>):</p> <pre><code>  volumes:\n  - name: mysql\n    secret:\n      secretName: lfsecret\n</code></pre> <p>Tip</p> <p>Pre-built version available: <code>kubectl create -f second-v3.yaml</code></p> </li> <li> <p>Delete, recreate, and verify the secret is accessible inside the container.</p> <pre><code>kubectl delete pod secondapp\nkubectl create -f second.yaml\nkubectl get pod secondapp\n</code></pre> <pre><code>NAME         READY   STATUS    RESTARTS   AGE\nsecondapp    1/1     Running   0          34s\n</code></pre> <pre><code>kubectl exec -ti secondapp -- /bin/sh\n</code></pre> <p>Inside:</p> <pre><code>/ $ cat /mysqlpassword/password\nLFTr@1n\n\n/ $ ls -al /mysqlpassword/\ntotal 4\ndrwxrwxrwt  ...  .\ndr-xr-xr-x  ...  ..\nlrwxrwxrwx  ...  ..data -&gt; ..2026_03_19_16_06_41.694089911\nlrwxrwxrwx  ...  password -&gt; ..data/password\n\n/ $ exit\n</code></pre> <p>The mount point uses symlinks - the actual data is in a timestamped directory, and <code>..data</code> is a symlink to it. This allows Kubernetes to atomically update secrets.</p> </li> </ol>"},{"location":"ch06_labs/#exercise-63-working-with-serviceaccounts","title":"Exercise 6.3: Working with ServiceAccounts","text":"<p>ServiceAccounts provide an identity for pod processes to interact with the Kubernetes API.</p> <ol> <li> <p>View existing secrets.</p> <pre><code>kubectl get secrets\nkubectl get secrets --all-namespaces\n</code></pre> </li> <li> <p>Create the ServiceAccount from <code>serviceaccount.yaml</code>.</p> <pre><code>kubectl create -f serviceaccount.yaml\n</code></pre> <pre><code>serviceaccount/secret-access-sa created\n</code></pre> <pre><code>kubectl get serviceaccounts\n</code></pre> <pre><code>NAME               SECRETS   AGE\ndefault            0         ...\nsecret-access-sa   0         34s\n</code></pre> </li> <li> <p>View existing ClusterRoles and compare <code>admin</code> vs <code>cluster-admin</code>.</p> <pre><code>kubectl get clusterroles\nkubectl get clusterroles admin -o yaml\nkubectl get clusterroles cluster-admin -o yaml\n</code></pre> <p>Note that <code>cluster-admin</code> uses <code>*</code> (wildcard) for all resources and verbs.</p> </li> <li> <p>Create the <code>secret-access-cr</code> ClusterRole from <code>clusterrole.yaml</code>. It grants only <code>get</code> and <code>list</code> on secrets.</p> <pre><code>kubectl create -f clusterrole.yaml\n</code></pre> <pre><code>clusterrole.rbac.authorization.k8s.io/secret-access-cr created\n</code></pre> <pre><code>kubectl get clusterrole secret-access-cr -o yaml\n</code></pre> </li> <li> <p>Bind the ClusterRole to the ServiceAccount using <code>rolebinding.yaml</code>.</p> <pre><code>kubectl create -f rolebinding.yaml\n</code></pre> <pre><code>rolebinding.rbac.authorization.k8s.io/secret-rb created\n</code></pre> <pre><code>kubectl get rolebindings\n</code></pre> <pre><code>NAME        AGE\nsecret-rb   17s\n</code></pre> </li> <li> <p>Verify the current <code>secondapp</code> pod uses the <code>default</code> serviceAccount.</p> <pre><code>kubectl get pod secondapp -o yaml | grep serviceAccount\n</code></pre> <pre><code>serviceAccount: default\nserviceAccountName: default\n</code></pre> </li> <li> <p>Edit <code>~/app2/second.yaml</code> and add <code>serviceAccountName: secret-access-sa</code> to the pod spec.</p> <pre><code>vim second.yaml\n</code></pre> <pre><code>spec:\n  serviceAccountName: secret-access-sa   # &lt;-- add this line\n  securityContext:\n    runAsUser: 1000\n</code></pre> <p>Tip</p> <p>Pre-built version available: <code>kubectl create -f second-v4.yaml</code></p> </li> <li> <p>Delete and recreate. Verify the new serviceAccount is in use.</p> <pre><code>kubectl delete pod secondapp\nkubectl create -f ~/app2/second.yaml\nkubectl get pod secondapp -o yaml | grep serviceAccount\n</code></pre> <pre><code>serviceAccount: secret-access-sa\nserviceAccountName: secret-access-sa\n</code></pre> </li> </ol>"},{"location":"ch06_labs/#exercise-64-implement-a-networkpolicy","title":"Exercise 6.4: Implement a NetworkPolicy","text":"<p>Calico enforces NetworkPolicies</p> <p>This cluster uses Calico, which fully enforces NetworkPolicies. Once you apply the policy in the next exercise, traffic will be blocked as described.</p> <ol> <li> <p>Review <code>allclosed.yaml</code>. This deny-all policy matches every pod and blocks both ingress and egress.</p> <pre><code>cat allclosed.yaml\n</code></pre> <pre><code>apiVersion: networking.k8s.io/v1\nkind: NetworkPolicy\nmetadata:\n  name: deny-default\nspec:\n  podSelector: {}\n  policyTypes:\n  - Ingress\n  - Egress\n</code></pre> </li> <li> <p>Delete the existing <code>secondapp</code> pod and rebuild it with a <code>webserver</code> nginx container and a label.</p> <pre><code>kubectl delete pod secondapp\nvim second.yaml\n</code></pre> <pre><code>spec:\n  serviceAccountName: secret-access-sa\n# securityContext:        # &lt;-- comment out\n#   runAsUser: 1000       # &lt;-- comment out\n  containers:\n  - name: webserver\n    image: nginx          # &lt;-- add this\n  - name: busy\n    image: busybox\n    command:\n</code></pre> <p>Also add <code>labels:</code> in the metadata section:</p> <pre><code>metadata:\n  name: secondapp\n  labels:\n    example: second\n</code></pre> <p>Tip</p> <p>Pre-built version available: <code>kubectl create -f second-v5.yaml</code></p> <pre><code>kubectl create -f second.yaml\nkubectl get pods\n</code></pre> <pre><code>NAME        READY   STATUS    RESTARTS   AGE\nsecondapp   2/2     Running   0          5s\n</code></pre> </li> <li> <p>Create the NodePort service and set it to port 32000.</p> <pre><code>kubectl create service nodeport secondapp --tcp=80\n</code></pre> <pre><code>service/secondapp created\n</code></pre> <p>Edit the service to change the selector to <code>example: second</code> and set the nodePort to <code>32000</code>.</p> <pre><code>kubectl edit svc secondapp\n</code></pre> </li> <li> <p>Verify the service and test access.</p> <pre><code>kubectl get svc secondapp\n</code></pre> <pre><code>NAME        TYPE       CLUSTER-IP      PORT(S)\nsecondapp   NodePort   10.97.96.75     80:32000/TCP\n</code></pre> <pre><code>curl http://10.97.96.75\n</code></pre> </li> <li> <p>Test egress from within the container.</p> <pre><code>kubectl exec -it -c busy secondapp -- sh\n</code></pre> <p>Inside:</p> <pre><code>/ $ nc -vz 127.0.0.1 80\n127.0.0.1 (127.0.0.1:80) open\n\n/ $ nc -vz www.linux.com 80\nwww.linux.com (151.101.185.5:80) open\n\n/ $ exit\n</code></pre> </li> </ol>"},{"location":"ch06_labs/#exercise-65-testing-the-networkpolicy","title":"Exercise 6.5: Testing the NetworkPolicy","text":"<p>Traffic will be blocked</p> <p>With Calico, the policy below will be enforced. Expect curl and nc tests to time out once the deny-all policy is applied.</p> <ol> <li> <p>Apply the deny-all policy.</p> <pre><code>kubectl create -f allclosed.yaml\n</code></pre> <pre><code>networkpolicy.networking.k8s.io/deny-default created\n</code></pre> </li> <li> <p>Test that traffic is now blocked.</p> <pre><code>curl http://10.97.96.75\nkubectl exec -it -c busy secondapp -- sh\n</code></pre> <pre><code>/ $ nc -vz www.linux.com 80\n/ $ exit\n</code></pre> </li> <li> <p>Update the policy to remove the Egress restriction and replace.</p> <pre><code>vim allclosed.yaml\n</code></pre> <pre><code>spec:\n  podSelector: {}\n  policyTypes:\n  - Ingress\n# - Egress    # &lt;-- comment out\n</code></pre> <pre><code>kubectl replace -f allclosed.yaml\n</code></pre> <pre><code>networkpolicy.networking.k8s.io/deny-default replaced\n</code></pre> <p>Tip</p> <p>Pre-built version available: <code>kubectl replace -f allclosed-v2.yaml</code></p> </li> <li> <p>Get the pod IP, then add an ingress rule to allow traffic from any pod.</p> <pre><code>kubectl get pod secondapp -o wide\n</code></pre> <p>Edit <code>allclosed.yaml</code> to add an ingress rule:</p> <pre><code>policyTypes:\n- Ingress\ningress:\n- from:\n  - podSelector: {}\n# - Egress\n</code></pre> <pre><code>kubectl replace -f allclosed.yaml\n</code></pre> <p>Tip</p> <p>Pre-built version available: <code>kubectl replace -f allclosed-v3.yaml</code></p> </li> <li> <p>Test pod-to-pod ingress with ping using a temporary alpine pod.</p> <pre><code>kubectl run -it test --rm=true --image alpine -- ping -c5 &lt;secondapp-pod-ip&gt;\n</code></pre> </li> <li> <p>Update the policy to only allow TCP port 80, blocking ICMP.</p> <pre><code>vim allclosed.yaml\n</code></pre> <pre><code>ingress:\n- from:\n  - podSelector: {}\n  ports:\n  - port: 80\n    protocol: TCP\n</code></pre> <pre><code>kubectl replace -f allclosed.yaml\n</code></pre> <p>Tip</p> <p>Pre-built version available: <code>kubectl replace -f allclosed-v4.yaml</code></p> <p>HTTP should be allowed:</p> <pre><code>curl http://10.97.96.75\n</code></pre> <p>ICMP should be blocked:</p> <pre><code>kubectl run -it test --rm=true --image alpine -- ping -c5 &lt;secondapp-pod-ip&gt;\n</code></pre> </li> <li> <p>Delete the NetworkPolicy so the registry and other pods remain accessible for future chapters.</p> <pre><code>kubectl delete networkpolicies deny-default\n</code></pre> <pre><code>networkpolicy.networking.k8s.io \"deny-default\" deleted\n</code></pre> </li> </ol>"},{"location":"ch06_labs/#exercise-66-domain-review","title":"Exercise 6.6: Domain Review","text":"<p>Important</p> <p>Source pages and content in this review can change at any time. Always check current information before your exam.</p> <p>Revisit the CKAD curriculum for topics covered in this chapter:</p> <ul> <li>Understand authentication, authorization, and admission control</li> <li>Create and consume Secrets</li> <li>Understand ServiceAccounts</li> <li>Understand SecurityContexts</li> <li> <p>Demonstrate basic understanding of NetworkPolicies</p> </li> <li> <p>Create a new deployment using the <code>nginx</code> image.</p> </li> <li>Create a <code>LoadBalancer</code> service to expose the deployment on port 80.</li> <li>Create a new NetworkPolicy called <code>netblock</code> that blocks all traffic to pods in this deployment only. Verify the object is created.</li> <li>Update <code>netblock</code> to allow ingress traffic on port 80 only. Verify with curl.</li> <li> <p>Create a pod using <code>security-review1.yaml</code>.</p> <pre><code>kubectl create -f security-review1.yaml\n</code></pre> </li> <li> <p>Check the pod status.</p> <pre><code>kubectl get pod securityreview\nkubectl describe pod securityreview\nkubectl logs securityreview\n</code></pre> </li> <li> <p>Exec into the container (or use <code>kubectl debug</code>) to find the UID that nginx actually needs. Hint: check <code>/etc/passwd</code> inside the nginx image for the <code>nginx</code> user's UID.</p> </li> <li>Edit <code>security-review1.yaml</code> and set the container <code>runAsUser</code> to the correct nginx UID. Recreate the pod and verify it runs.</li> <li> <p>Create a new ServiceAccount called <code>securityaccount</code>.</p> <pre><code>kubectl create serviceaccount securityaccount\n</code></pre> </li> <li> <p>Create a ClusterRole named <code>secrole</code> that allows <code>create</code>, <code>delete</code>, and <code>list</code> of pods in all apiGroups.</p> </li> <li>Bind <code>secrole</code> to <code>securityaccount</code>.</li> <li> <p>Create a token for <code>securityaccount</code> and save only the token value to <code>/tmp/securitytoken</code>.</p> <pre><code>kubectl create token securityaccount &gt; /tmp/securitytoken\n</code></pre> </li> <li> <p>Clean up all resources created during this review.</p> <pre><code>kubectl delete deployment nginx --ignore-not-found\nkubectl delete svc nginx --ignore-not-found\nkubectl delete networkpolicy netblock --ignore-not-found\nkubectl delete pod securityreview --ignore-not-found\nkubectl delete serviceaccount securityaccount --ignore-not-found\nkubectl delete clusterrole secrole --ignore-not-found\nkubectl delete clusterrolebinding secrole --ignore-not-found\n</code></pre> </li> </ul>"},{"location":"ch07_labs/","title":"Chapter 7 - Exposing Applications","text":""},{"location":"ch07_labs/#lab-overview","title":"Lab Overview","text":"<p>In this chapter you will work with all four service types (ClusterIP, NodePort, LoadBalancer, ExternalName), explore CoreDNS service discovery across namespaces, deploy an NGINX ingress controller via Helm, configure ingress rules for multiple virtual hosts, and optionally install Linkerd as a service mesh.</p> <p>Prerequisites</p> <p>This chapter continues from Chapter 6. The <code>secondapp</code> pod (with <code>webserver</code> nginx and <code>busy</code> busybox containers, label <code>example: second</code>) should be running. If not, recreate it from <code>~/app2/second.yaml</code>.</p>"},{"location":"ch07_labs/#exercise-71-expose-a-service","title":"Exercise 7.1: Expose a Service","text":""},{"location":"ch07_labs/#clusterip-internal-access-only","title":"ClusterIP - Internal Access Only","text":"<ol> <li> <p>Connect to the controller node and move into the chapter lab directory.</p> <pre><code>cd ~/lfd459/ch07-exposing-apps\nkubectl get svc\n</code></pre> <pre><code>NAME         TYPE        CLUSTER-IP       PORT(S)\nkubernetes   ClusterIP   10.96.0.1        443/TCP\nnginx        ClusterIP   10.108.95.67     443/TCP\nregistry     ClusterIP   10.105.119.236   5000/TCP\nsecondapp    NodePort    10.111.26.8      80:32000/TCP\n</code></pre> </li> <li> <p>Save the existing <code>secondapp</code> service definition and delete it.</p> <pre><code>kubectl get svc secondapp -o yaml &gt; oldservice.yaml\nkubectl delete svc secondapp\n</code></pre> <pre><code>service \"secondapp\" deleted\n</code></pre> </li> <li> <p>Recreate the service using <code>newservice.yaml</code>. Note that no <code>type</code> is specified - it defaults to <code>ClusterIP</code>.</p> <pre><code>kubectl create -f newservice.yaml\n</code></pre> <pre><code>service/secondapp created\n</code></pre> <pre><code>kubectl get svc secondapp\n</code></pre> <pre><code>NAME        TYPE        CLUSTER-IP      PORT(S)   AGE\nsecondapp   ClusterIP   10.98.148.52    80/TCP    14s\n</code></pre> </li> <li> <p>Test access to the ClusterIP. You should see the nginx welcome page.</p> <pre><code>curl http://10.98.148.52\n</code></pre> </li> </ol>"},{"location":"ch07_labs/#service-update-pattern","title":"Service Update Pattern","text":"<ol> <li> <p>Create a second web server using the <code>httpd</code> image. Its default page differs from nginx.</p> <pre><code>kubectl create deployment newserver --image=httpd\n</code></pre> <pre><code>deployment.apps/newserver created\n</code></pre> </li> <li> <p>Find the label that <code>newserver</code>'s pods use.</p> <pre><code>kubectl get deployment newserver -o wide\n</code></pre> <p>Note the <code>SELECTOR</code> column - it will be <code>app=newserver</code>.</p> </li> <li> <p>Edit the <code>secondapp</code> service to switch the selector to <code>newserver</code>'s label.</p> <pre><code>kubectl edit svc secondapp\n</code></pre> <p>Change <code>selector.example: second</code> to <code>selector.app: newserver</code>.</p> </li> <li> <p>Test - traffic should now show the httpd default page instead of nginx.</p> <pre><code>curl http://10.98.148.52\n</code></pre> <pre><code>&lt;html&gt;&lt;body&gt;&lt;h1&gt;It works!&lt;/h1&gt;&lt;/body&gt;&lt;/html&gt;\n</code></pre> </li> <li> <p>Edit the selector back to <code>example: second</code> and test again - nginx returns. Then delete the <code>newserver</code> deployment.</p> <pre><code>kubectl delete deployment newserver\n</code></pre> </li> </ol>"},{"location":"ch07_labs/#nodeport-external-access","title":"NodePort - External Access","text":"<ol> <li> <p>Edit <code>newservice.yaml</code> to add a NodePort type and pin the high port to <code>32000</code>.</p> <pre><code>vim newservice.yaml\n</code></pre> <p>Add the <code>nodePort</code> and <code>type</code> fields inside <code>ports</code>:</p> <pre><code>spec:\n  ports:\n  - port: 80\n    protocol: TCP\n    nodePort: 32000    # &lt;-- add\n  type: NodePort        # &lt;-- add\n  selector:\n    example: second\n</code></pre> <p>Tip</p> <p>Pre-built version available: <code>kubectl create -f newservice-v2.yaml</code></p> </li> <li> <p>Delete and recreate the service. Note a new ClusterIP is assigned.</p> <pre><code>kubectl delete svc secondapp\nkubectl create -f newservice.yaml\nkubectl get svc secondapp\n</code></pre> <pre><code>NAME        TYPE       CLUSTER-IP       PORT(S)\nsecondapp   NodePort   10.109.134.221   80:32000/TCP\n</code></pre> </li> <li> <p>Test via the ClusterIP (internal) and via any node IP on port 32000 (external).</p> <pre><code>curl http://10.109.134.221\ncurl http://worker1:32000\ncurl http://worker2:32000\n</code></pre> </li> </ol>"},{"location":"ch07_labs/#loadbalancer","title":"LoadBalancer","text":"<ol> <li> <p>Edit <code>newservice.yaml</code> and change <code>type: NodePort</code> to <code>type: LoadBalancer</code>.</p> <pre><code>vim newservice.yaml\n</code></pre> <pre><code>type: LoadBalancer    # &lt;-- change from NodePort\n</code></pre> <p>Tip</p> <p>Pre-built version available: <code>kubectl create -f newservice-v3.yaml</code></p> <pre><code>kubectl delete svc secondapp\nkubectl create -f newservice.yaml\n</code></pre> </li> <li> <p>Check the service. <code>EXTERNAL-IP</code> will remain <code>&lt;pending&gt;</code> - there is no cloud load balancer in this environment. The NodePort still works.</p> <pre><code>kubectl get svc secondapp\n</code></pre> <pre><code>NAME        TYPE           CLUSTER-IP      EXTERNAL-IP   PORT(S)\nsecondapp   LoadBalancer   10.109.26.21    &lt;pending&gt;     80:32000/TCP\n</code></pre> <p>Access still works via the NodePort.</p> <pre><code>curl http://worker1:32000\n</code></pre> </li> </ol>"},{"location":"ch07_labs/#coredns-service-discovery","title":"CoreDNS Service Discovery","text":"<ol> <li> <p>Log into the <code>busy</code> container of <code>secondapp</code> and explore DNS resolution.</p> <pre><code>kubectl exec -it secondapp -c busy -- sh\n</code></pre> <p>Inside the container:</p> <pre><code># Resolve the secondapp service - only works for services in the same namespace\n/ $ nslookup secondapp\nName: secondapp.default.svc.cluster.local\nAddress: 10.96.214.133\n\n# Resolve the registry service\n/ $ nslookup registry\nName: registry.default.svc.cluster.local\nAddress: 10.110.95.21\n\n# Reverse-lookup the DNS server IP\n/ $ nslookup 10.96.0.10\n10.0.96.10.in-addr.arpa  name = kube-dns.kube-system.svc.cluster.local\n\n# Short name search fails (not in default namespace)\n/ $ nslookup kube-dns\n** server can't find kube-dns.default.svc.cluster.local: NXDOMAIN\n\n# FQDN works\n/ $ nslookup kube-dns.kube-system.svc.cluster.local\nName: kube-dns.kube-system.svc.cluster.local\nAddress: 10.96.0.10\n\n/ $ exit\n</code></pre> <p>Note</p> <p>CoreDNS appends <code>.default.svc.cluster.local</code> when resolving short names. Services in other namespaces require the FQDN: <code>&lt;service&gt;.&lt;namespace&gt;.svc.cluster.local</code>.</p> </li> </ol>"},{"location":"ch07_labs/#cross-namespace-dns","title":"Cross-Namespace DNS","text":"<ol> <li> <p>Create a new namespace <code>multitenant</code> with a deployment and service.</p> <pre><code>kubectl create ns multitenant\nkubectl -n multitenant create deployment mainapp --image=nginx\nkubectl -n multitenant expose deployment mainapp \\\n</code></pre> <pre><code>--name=shopping --type=NodePort --port=80\n</code></pre> </li> <li> <p>Log back into the <code>busy</code> container and test cross-namespace DNS.</p> <pre><code>kubectl exec -it secondapp -c busy -- sh\n</code></pre> <p>Inside:</p> <pre><code># Short name fails - shopping is not in the default namespace\n/ $ nslookup shopping\n** server can't find shopping.default.svc.cluster.local: NXDOMAIN\n\n# FQDN resolves correctly\n/ $ nslookup shopping.multitenant.svc.cluster.local\nName: shopping.multitenant.svc.cluster.local\nAddress: 10.101.4.142\n\n# Partial FQDN with namespace works for wget\n/ $ wget -O - shopping.multitenant\n&lt;!DOCTYPE html&gt;\n&lt;html&gt;\n&lt;head&gt;&lt;title&gt;Welcome to nginx!&lt;/title&gt;\n...\n\n/ $ exit\n</code></pre> </li> </ol>"},{"location":"ch07_labs/#exercise-72-service-mesh-and-ingress-controller","title":"Exercise 7.2: Service Mesh and Ingress Controller","text":""},{"location":"ch07_labs/#nginx-ingress-controller-via-helm","title":"NGINX Ingress Controller via Helm","text":"<p>An ingress controller allows you to route traffic to multiple services using a single entry point based on HTTP host headers or paths.</p> <ol> <li> <p>Make sure Helm is available.</p> <pre><code>helm version\n</code></pre> <p>If not found, install it:</p> <p>Helm version</p> <p>Check the latest stable release at https://github.com/helm/helm/releases before downloading. Replace <code>v3.18.1</code> below with the current version if it differs.</p> <pre><code>wget https://get.helm.sh/helm-v3.18.1-linux-amd64.tar.gz\ntar -xf helm-v3.18.1-linux-amd64.tar.gz\nsudo cp linux-amd64/helm /usr/local/bin/\n</code></pre> </li> <li> <p>Add the NGINX ingress Helm repository and update.</p> <pre><code>helm repo add ingress-nginx https://kubernetes.github.io/ingress-nginx\nhelm repo update\n</code></pre> </li> <li> <p>Download the chart and edit <code>values.yaml</code> to use a <code>DaemonSet</code> instead of a <code>Deployment</code> (one ingress pod per node).</p> <pre><code>helm fetch ingress-nginx/ingress-nginx --untar\ncd ingress-nginx\nvim values.yaml\n</code></pre> <p>Find the <code>kind:</code> line around line 150 and change it:</p> <pre><code>## DaemonSet or Deployment\nkind: DaemonSet    # &lt;-- change from Deployment\n</code></pre> </li> <li> <p>Install the ingress controller using the local chart.</p> <pre><code>helm install myingress .\n</code></pre> <pre><code>NAME: myingress\nSTATUS: deployed\n</code></pre> </li> <li> <p>Verify the controller pods are running - one per node (DaemonSet).</p> <pre><code>kubectl get pods --all-namespaces | grep myingress\n</code></pre> <pre><code>default   myingress-ingress-nginx-controller-xxxxx   1/1   Running   0   20s   controller\ndefault   myingress-ingress-nginx-controller-yyyyy   1/1   Running   0   20s   worker1\ndefault   myingress-ingress-nginx-controller-zzzzz   1/1   Running   0   20s   worker2\n</code></pre> </li> <li> <p>Check the ingress controller service. <code>EXTERNAL-IP</code> will be <code>&lt;pending&gt;</code> - that's expected.</p> <pre><code>kubectl get svc | grep myingress\n</code></pre> <pre><code>myingress-ingress-nginx-controller   LoadBalancer   10.104.227.79   &lt;pending&gt;   80:32558/TCP,443:30219/TCP\n</code></pre> </li> <li> <p>Note the pod IPs of the ingress controller pods (these are the IPs you'll use directly for testing).</p> <pre><code>kubectl get pod -o wide | grep myingress\n</code></pre> </li> </ol>"},{"location":"ch07_labs/#creating-an-ingress-rule","title":"Creating an Ingress Rule","text":"<ol> <li> <p>Review <code>ingress.yaml</code> in the chapter directory. It routes traffic for <code>Host: www.example.com</code> to the <code>secondapp</code> service.</p> <pre><code>cat ingress.yaml\n</code></pre> <pre><code>apiVersion: networking.k8s.io/v1\nkind: Ingress\nmetadata:\n  name: ingress-test\n  annotations:\n    nginx.ingress.kubernetes.io/service-upstream: \"true\"\n  namespace: default\nspec:\n  ingressClassName: nginx\n  rules:\n  - host: www.example.com\n    http:\n      paths:\n      - backend:\n          service:\n            name: secondapp\n            port:\n              number: 80\n        path: /\n        pathType: ImplementationSpecific\n</code></pre> <p>Old ingress file</p> <p>A file called <code>ingress_rule.yaml</code> exists in the lab files - it uses <code>apiVersion: networking.k8s.io/v1beta1</code> which was removed in Kubernetes 1.22. Do not use it. Always use <code>ingress.yaml</code> (v1).</p> </li> <li> <p>Copy and apply the ingress rule.</p> <pre><code>kubectl create -f ~/lfd459/ch07-exposing-apps/ingress.yaml\n</code></pre> <pre><code>ingress.networking.k8s.io/ingress-test created\n</code></pre> <pre><code>kubectl get ingress\n</code></pre> <pre><code>NAME           CLASS   HOSTS             ADDRESS   PORTS   AGE\ningress-test   nginx   www.example.com             80      5s\n</code></pre> </li> <li> <p>Test the ingress. Without the correct <code>Host</code> header you get a 404. With the header, nginx responds.</p> <pre><code># Get the ingress controller pod IP on the controller node\nINGRESS_IP=$(kubectl get pods -o wide | \\\ngrep myingress | grep controller | awk 'NR==1{print $6}')\n</code></pre> <pre><code>echo $INGRESS_IP\n</code></pre> <pre><code># Without Host header - 404 (ingress has no default backend)\n</code></pre> <pre><code>curl $INGRESS_IP\n</code></pre> <pre><code>&lt;html&gt;&lt;head&gt;&lt;title&gt;404 Not Found&lt;/title&gt;&lt;/head&gt;...&lt;/html&gt;\n\n# With matching Host header - nginx welcome page\n</code></pre> <pre><code>curl -H \"Host: www.example.com\" http://$INGRESS_IP\n</code></pre> <pre><code>&lt;!DOCTYPE html&gt;\n&lt;html&gt;&lt;head&gt;&lt;title&gt;Welcome to nginx!&lt;/title&gt;...\n</code></pre> <p>Using ClusterIP instead</p> <p>You can also use the ingress controller's ClusterIP with the Host header: <pre><code>curl -H \"Host: www.example.com\" http://10.104.227.79\n</code></pre></p> </li> </ol>"},{"location":"ch07_labs/#adding-a-second-virtual-host","title":"Adding a Second Virtual Host","text":"<ol> <li> <p>Deploy a third web server and customise its default page.</p> <p><code>bash</code></p> <pre><code>```bash\n</code></pre> <p>kubectl create deployment thirdpage --image=nginx kubectl expose deployment thirdpage --port=80 --type=NodePort     ```</p> </li> <li> <p>Label the pod so it can be targeted (use Tab completion for the pod name).</p> <pre><code>kubectl label pod thirdpage-&lt;Tab&gt; example=third\n</code></pre> </li> <li> <p>Exec into the pod and edit the nginx default page to say \"Third Page\".</p> <pre><code>kubectl exec -it thirdpage-&lt;Tab&gt; -- /bin/bash\n</code></pre> <p>Inside:</p> <pre><code>root@thirdpage:/# apt-get update -qq &amp;&amp; apt-get install vim -y -qq\nroot@thirdpage:/# vim /usr/share/nginx/html/index.html\n</code></pre> <p>Change <code>&lt;title&gt;Welcome to nginx!&lt;/title&gt;</code> to <code>&lt;title&gt;Third Page&lt;/title&gt;</code>, then exit.</p> </li> <li> <p>Edit the ingress resource to add a second host rule for <code>thirdpage.org</code>.</p> <pre><code>kubectl edit ingress ingress-test\n</code></pre> <p>Add a second rule block under <code>spec.rules</code>:</p> <pre><code>spec:\n  rules:\n  - host: thirdpage.org\n    http:\n      paths:\n      - backend:\n          service:\n            name: thirdpage\n            port:\n              number: 80\n        path: /\n        pathType: ImplementationSpecific\n  - host: www.example.com\n    http:\n      paths:\n      - backend:\n          service:\n            name: secondapp\n            port:\n              number: 80\n        path: /\n        pathType: ImplementationSpecific\n</code></pre> <p>Tip</p> <p>Pre-built version available: <code>kubectl replace -f ingress-v2.yaml</code></p> </li> <li> <p>Test both virtual hosts.</p> <pre><code># Should show \"Third Page\"\n</code></pre> <pre><code>curl -H \"Host: thirdpage.org\" http://$INGRESS_IP\n</code></pre> <pre><code>&lt;!DOCTYPE html&gt;&lt;html&gt;&lt;head&gt;&lt;title&gt;Third Page&lt;/title&gt;...\n\n# Should still show nginx default\n</code></pre> <pre><code>curl -H \"Host: www.example.com\" http://$INGRESS_IP\n</code></pre> <pre><code>&lt;!DOCTYPE html&gt;&lt;html&gt;&lt;head&gt;&lt;title&gt;Welcome to nginx!&lt;/title&gt;...\n</code></pre> </li> <li> <p>Consider: how would you switch <code>thirdpage.org</code> traffic to an <code>httpd</code> server by editing only the ingress rule - without touching the pods or services?</p> </li> </ol>"},{"location":"ch07_labs/#service-mesh","title":"Service Mesh","text":"<p>A service mesh is an infrastructure layer that handles service-to-service communication inside a cluster. Rather than building resilience logic (retries, timeouts, mutual TLS, circuit breaking) into each application, a service mesh offloads it to a sidecar proxy injected alongside every pod.</p> <p>Key capabilities a service mesh provides:</p> <ul> <li>Mutual TLS (mTLS) - encrypts and authenticates all traffic between services automatically</li> <li>Observability - metrics, traces, and traffic topology without application changes</li> <li>Traffic management - canary releases, traffic splitting, retries, timeouts at the network level</li> <li>Circuit breaking - automatically stop sending traffic to failing services</li> </ul> <p>Popular implementations include Linkerd, Istio, and Cilium Service Mesh. They all follow the same pattern: a control plane that manages configuration, and data plane sidecar proxies (typically Envoy or a lightweight alternative) that intercept pod traffic.</p> <p>CKAD exam scope</p> <p>The CKAD exam does not test service mesh configuration. You are expected to understand what a service mesh is and the problem it solves, not how to install or operate one.</p>"},{"location":"ch07_labs/#exercise-73-domain-review","title":"Exercise 7.3: Domain Review","text":"<p>Important</p> <p>Source pages and content in this review can change at any time. Always check current information before your exam.</p> <p>Revisit the CKAD curriculum for topics covered in this chapter:</p> <ul> <li>Use Kubernetes primitives to implement common deployment strategies (blue/green or canary)</li> <li>Use the Helm package manager to deploy existing packages</li> <li>Provide and troubleshoot access to applications via services</li> <li> <p>Use Ingress rules to expose applications</p> </li> <li> <p>Create a new pod called <code>webone</code> running nginx. Expose port 80.</p> </li> <li>Create a service named <code>webone-svc</code> accessible from outside the cluster.</li> <li>Update both pod and service with matching selectors so traffic reaches the webserver.</li> <li>Change the service type so it is only accessible from within the cluster. Verify external access no longer works but internal curl does.</li> <li>Deploy another pod called <code>webtwo</code> running the <code>wlniao/website</code> image. Create a service <code>webtwo-svc</code> accessible only from within the cluster. Verify the default pages for each server are different.</li> <li>Install and configure an ingress controller so that requests for <code>webone.com</code> show the nginx default page and requests for <code>webtwo.org</code> show the <code>wlniao/website</code> default page.</li> <li> <p>Clean up all resources created in this review.</p> <pre><code>kubectl delete pod webone webtwo --ignore-not-found\nkubectl delete svc webone-svc webtwo-svc --ignore-not-found\nkubectl delete ingress --all --ignore-not-found\n</code></pre> </li> </ul>"},{"location":"ch08_labs/","title":"Chapter 8 - Application Troubleshooting","text":""},{"location":"ch08_labs/#lab-overview","title":"Lab Overview","text":"<p>This final chapter covers the troubleshooting flow for Kubernetes applications - from pod inspection to service endpoints to kube-proxy. You will work with API deprecations, use <code>kubectl debug</code> with ephemeral containers, and tackle a multi-bug deployment as a domain review.</p>"},{"location":"ch08_labs/#exercise-81-monitor-applications","title":"Exercise 8.1: Monitor Applications","text":"<p>A systematic troubleshooting flow is more valuable than memorising specific errors. Start from the application, work outward to the cluster.</p> <ol> <li> <p>Connect to the controller node. Check the <code>secondapp</code> pod status.</p> <pre><code>kubectl get pods secondapp\n</code></pre> <pre><code>NAME        READY   STATUS    RESTARTS   AGE\nsecondapp   2/2     Running   49         2d\n</code></pre> <p>The pod is running. <code>busy</code> has many restarts - expected, as <code>sleep 3600</code> completes every hour. <code>webserver</code> (nginx) should have 0 restarts.</p> </li> <li> <p>Describe the pod in detail.</p> <pre><code>kubectl describe pod secondapp\n</code></pre> <p>Check:</p> <ul> <li>Both containers show <code>State: Running</code></li> <li><code>webserver</code> has <code>Restart Count: 0</code></li> <li><code>Conditions</code> all show <code>True</code>: <code>Initialized</code>, <code>Ready</code>, <code>PodScheduled</code></li> </ul> </li> <li> <p>Check pod <code>Conditions</code> specifically.</p> <pre><code>kubectl describe pod secondapp | grep -A8 \"Conditions:\"\n</code></pre> <pre><code>Conditions:\n  Type              Status\n  Initialized       True\n  Ready             True\n  PodScheduled      True\n</code></pre> </li> <li> <p>Scan the Events section for any <code>Warning</code> entries.</p> <pre><code>kubectl describe pod secondapp | grep -A20 \"^Events:\"\n</code></pre> </li> <li> <p>View the logs for each container.</p> <pre><code>kubectl logs secondapp webserver\nkubectl logs secondapp busy\n</code></pre> </li> <li> <p>Exec into <code>busy</code> and test DNS and external connectivity.</p> <pre><code>kubectl exec -it secondapp -c busy -- sh\n</code></pre> <p>Inside:</p> <pre><code>/ $ nslookup www.linuxfoundation.org\nServer:     10.96.0.10\nName:       www.linuxfoundation.org\nAddress: 23.185.0.2\n\n/ $ cat /etc/resolv.conf\nnameserver 10.96.0.10\nsearch default.svc.cluster.local svc.cluster.local cluster.local\n\n/ $ exit\n</code></pre> </li> <li> <p>Verify services and their selectors.</p> <pre><code>kubectl get svc\nkubectl get svc secondapp -o yaml | grep -A5 \"selector:\"\n</code></pre> <p>Confirm <code>selector.example: second</code> matches the pod label <code>example=second</code>.</p> </li> <li> <p>Verify the endpoint object exists and has the correct pod IP and port.</p> <pre><code>kubectl get ep\n</code></pre> <pre><code>NAME         ENDPOINTS\nsecondapp    192.168.x.y:80\n...\n</code></pre> <pre><code>kubectl get ep secondapp -o yaml\n</code></pre> </li> <li> <p>Check kube-proxy logs.</p> <pre><code>kubectl -n kube-system get pod | grep proxy\nkubectl -n kube-system logs kube-proxy-&lt;TAB&gt;\n</code></pre> </li> <li> <p>Verify kube-proxy has created iptables rules for the service.</p> <pre><code>sudo iptables-save | grep secondapp\n</code></pre> <p>You should see rules referencing port <code>32000</code> (NodePort) and the service ClusterIP.</p> </li> <li> <p>Test via localhost NodePort to confirm the full proxy chain is working.</p> <pre><code>curl localhost:32000\n</code></pre> </li> </ol>"},{"location":"ch08_labs/#exercise-82-update-a-deprecated-yaml-file","title":"Exercise 8.2: Update a Deprecated YAML File","text":"<p>The Kubernetes API evolves. YAML that worked on older clusters may fail on newer ones due to API removals.</p> <ol> <li> <p>Move into the chapter lab directory and try to create the broken deployment.</p> <pre><code>cd ~/lfd459/ch08-troubleshooting\nkubectl create -f brokendeploy.yaml\n</code></pre> <p>The command will fail immediately. Read the error carefully.</p> </li> <li> <p>Create a working deployment from scratch to understand what the current API requires.</p> <pre><code>kubectl create deployment reference --image=nginx --dry-run=client -o yaml\n</code></pre> <p>Compare the output with <code>brokendeploy.yaml</code>.</p> </li> <li> <p>Identify and fix all issues in <code>brokendeploy.yaml</code>. There are at least three:</p> <p>Issues to find</p> <ul> <li>The <code>apiVersion</code> field references an API group that no longer exists</li> <li>The <code>selector.matchLabels</code> field is missing entirely (required for <code>apps/v1</code> Deployments)</li> <li>The pod template labels do not match the deployment's own labels</li> </ul> </li> <li> <p>Edit the file and apply fixes.</p> <pre><code>vim brokendeploy.yaml\n</code></pre> </li> <li> <p>Create the deployment and verify the pod starts successfully.</p> <pre><code>kubectl create -f brokendeploy.yaml\nkubectl get deployment broken\nkubectl get pods -l app=broken\n</code></pre> </li> <li> <p>Clean up.</p> <pre><code>kubectl delete deployment broken\n</code></pre> </li> </ol> Fixes revealed <ol> <li><code>apiVersion: extensions/v1beta1</code> -&gt; <code>apiVersion: apps/v1</code> (removed in Kubernetes 1.16)</li> <li>Add <code>selector.matchLabels: {app: broken}</code> under <code>spec:</code> - mandatory for <code>apps/v1</code></li> <li>Change pod template label from <code>app: thirdpage</code> to <code>app: broken</code> so the selector matches</li> </ol>"},{"location":"ch08_labs/#exercise-83-troubleshooting-with-ephemeral-containers","title":"Exercise 8.3: Troubleshooting with Ephemeral Containers","text":"<p>Ephemeral containers allow you to attach a debugging shell to a running pod - even if the original container image has no shell.</p> <ol> <li> <p>Deploy the broken application.</p> <pre><code>kubectl apply -f brokenapp.yaml\n</code></pre> <pre><code>pod/nginx-debug-pod created\nservice/nginx-debug-svc created\n</code></pre> </li> <li> <p>Check the pod status. It will show <code>0/1 READY</code>.</p> <pre><code>kubectl get all\n</code></pre> <pre><code>NAME                   READY   STATUS    RESTARTS   AGE\npod/nginx-debug-pod    0/1     Running   0          5s\n\nNAME                     TYPE        CLUSTER-IP       PORT(S)   AGE\nservice/nginx-debug-svc  ClusterIP   10.111.195.30    80/TCP    5s\n</code></pre> </li> <li> <p>Try to connect to the service - it will fail as the pod has no endpoints yet.</p> <pre><code>kubectl get ep nginx-debug-svc\n</code></pre> <pre><code>NAME              ENDPOINTS   AGE\nnginx-debug-svc   &lt;none&gt;      10s\n</code></pre> <p>Get the ClusterIP first:</p> <pre><code>kubectl get svc nginx-debug-svc\n</code></pre> <pre><code>curl &lt;CLUSTER-IP&gt;\n</code></pre> <pre><code>curl: (7) Failed to connect to &lt;CLUSTER-IP&gt; port 80\n</code></pre> </li> <li> <p>Review the pod's probes.</p> <pre><code>cat brokenapp.yaml\n</code></pre> <p>Two things are happening:</p> <ul> <li>The startupProbe runs <code>rm -f /bin/bash</code> - it deletes the bash shell from the container</li> <li>The readinessProbe checks for <code>/tmp/app.log</code> - this file does not exist yet</li> </ul> </li> <li> <p>Try to exec directly into the pod - it will fail because <code>/bin/bash</code> has been removed.</p> <pre><code>kubectl exec -it pod/nginx-debug-pod -- bash\n</code></pre> <pre><code>error: Internal error occurred: exec: \"bash\": executable file not found in $PATH\n</code></pre> </li> <li> <p>Attach an ephemeral container using <code>kubectl debug</code>.</p> <pre><code>kubectl debug pod/nginx-debug-pod -it \\\n--image=busybox --target=nginx -- /bin/sh\n</code></pre> </li> <li> <p>Inside the debug container, verify nginx is running as PID 1.</p> <pre><code># ps aux\nPID   USER   COMMAND\n1     root   nginx: master process nginx -g daemon off;\n29    root   nginx: worker process\n30    root   sh\n</code></pre> </li> <li> <p>Confirm you are inside the same pod namespace.</p> <pre><code># hostname\nnginx-debug-pod\n</code></pre> </li> <li> <p>Create the missing <code>/tmp/app.log</code> file by writing through <code>/proc/1/root</code>.</p> <pre><code># echo \"hello from debug\" &gt; /proc/1/root/tmp/app.log\n# ls -l /proc/1/root/tmp/\n-rw-r--r-- 1 root root 17 ... app.log\n# cat /proc/1/root/tmp/app.log\nhello from debug\n# exit\n</code></pre> </li> <li> <p>The readiness probe succeeds within the next <code>periodSeconds</code>. Verify the pod is now Ready.</p> <pre><code>kubectl get all\n</code></pre> <pre><code>NAME                   READY   STATUS    RESTARTS   AGE\npod/nginx-debug-pod    1/1     Running   0          2m\n\nNAME                     TYPE        CLUSTER-IP      ENDPOINTS\nservice/nginx-debug-svc  ClusterIP   10.111.195.30   10.244.x.y:80\n</code></pre> </li> <li> <p>Test service access.</p> <pre><code>curl &lt;CLUSTER-IP&gt;\n</code></pre> <pre><code>&lt;!DOCTYPE html&gt;\n&lt;html&gt;&lt;head&gt;&lt;title&gt;Welcome to nginx!&lt;/title&gt;&lt;/head&gt;...\n</code></pre> </li> <li> <p>Clean up.</p> <pre><code>kubectl delete -f brokenapp.yaml\n</code></pre> </li> </ol> <p>Key takeaway</p> <p><code>kubectl debug</code> with ephemeral containers is the correct tool when the container has no shell, the image is distroless, or when you need to debug without modifying the running container. The <code>--target</code> flag makes the ephemeral container share the target container's process namespace.</p>"},{"location":"ch08_labs/#exercise-84-conformance-testing-optional","title":"Exercise 8.4: Conformance Testing (Optional)","text":"<p>Optional</p> <p>Sonobuoy conformance testing takes 60+ minutes to complete and is not required for the CKAD exam.</p> <pre><code># Download the latest release\ncurl -sLO \\\nhttps://github.com/vmware-tanzu/sonobuoy/releases/latest/download/sonobuoy_linux_amd64.tar.gz\ntar -xvf sonobuoy_linux_amd64.tar.gz\nsudo mv sonobuoy /usr/local/bin/\n\n# Run tests\nsonobuoy run --wait\n\n# Monitor progress in a second terminal\nsonobuoy status\nsonobuoy logs\n\n# Retrieve and view results\nsonobuoy retrieve\nsonobuoy results &lt;tarball-file&gt;\n\n# Clean up\nsonobuoy delete --wait\n</code></pre>"},{"location":"ch08_labs/#exercise-85-domain-review","title":"Exercise 8.5: Domain Review","text":"<p>Important</p> <p>Source pages and content in this review can change at any time. Always check current information before your exam.</p> <p>Revisit the CKAD curriculum for topics covered in this chapter:</p> <ul> <li>Understand API deprecations</li> <li>Use provided tools to monitor Kubernetes applications</li> <li> <p>Debugging in Kubernetes</p> </li> <li> <p>Use <code>troubleshoot-review1.yaml</code> to create a deployment. The create command will fail immediately.</p> <pre><code>kubectl create -f troubleshoot-review1.yaml\n</code></pre> </li> <li> <p>Read the error carefully. Then inspect the file and identify all the problems. There are at least four distinct issues.</p> <pre><code>cat troubleshoot-review1.yaml\n</code></pre> <p>Where to look</p> <p>Check the <code>apiVersion</code>, <code>replicas</code>, <code>selector.matchLabels</code> vs pod template labels, and the resource <code>requests</code> vs <code>limits</code>.</p> </li> <li> <p>Fix each issue and recreate until the deployment runs a single pod for at least one minute without errors.</p> <pre><code>kubectl get deploy igottrouble\n</code></pre> <pre><code>NAME          READY   UP-TO-DATE   AVAILABLE   AGE\nigottrouble   1/1     1            1           5m13s\n</code></pre> </li> <li> <p>Clean up.</p> <pre><code>kubectl delete deploy igottrouble --ignore-not-found\n</code></pre> </li> </ul> Fixes revealed <ol> <li><code>apiVersion: extensions/v1</code> -&gt; <code>apiVersion: apps/v1</code> (invalid group)</li> <li><code>replicas: 0</code> -&gt; <code>replicas: 1</code></li> <li><code>selector.matchLabels: run: ugottrouble</code> -&gt; <code>run: igottrouble</code> (typo)</li> <li>CPU <code>requests: 2.5</code> exceeds <code>limits: 1</code> - fix: <code>requests.cpu: \"0.5\"</code></li> </ol>"},{"location":"ch08_labs/#course-complete","title":"Course Complete","text":"<p>Congratulations on completing all LFD459 lab chapters. Before sitting the CKAD exam, make sure you:</p> <ul> <li>Can create, edit, and troubleshoot all object types covered without referencing notes</li> <li>Are familiar with the three documentation sources allowed during the exam</li> <li>Have practised the domain review exercises at speed</li> <li>Have reviewed the current CKAD curriculum at https://www.cncf.io/certification/ckad/</li> </ul> <p>Good luck!</p>"}]}